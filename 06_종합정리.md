# 종합정리 — 기존계약-신규상품 차이 비교 AI 지원

> 개인적으로 직접 정의한 과제입니다. 이 문제를 선택한 근거는 아래 1번에 있습니다. 상세
> 근거는 [`01_문제정의.md`](01_문제정의.md) 참고.

## 1. 문제

기존 보험계약을 신규 상품으로 전환할 때, 보험료 외에 보장·지급조건·갱신·환급금 같은 "눈에 잘
안 띄는" 조건 변화를 놓치면 고객이 손해를 볼 수 있습니다. AI는 이 차이를 근거와 함께 찾아
사람이 확인할 수 있도록 하는 역할까지 하고, 최종 판단(가입/전환 여부)은 설계사가 합니다.

**왜 지금 이 문제인가**: 금융위원회가 2025-01-22 공식 발표(원문 직접 확인, `01_문제정의.md`
§1)에서 GA(법인보험대리점)의 "기존계약-신규계약 중요사항 비교 미고지"를 반복되는 불완전판매
문제로 지목했습니다. 2024-09-23 금감원 검사 결과(언론 9곳 교차확인, 원문 미확인 — 숨기지
않음)에 따르면 5개 대형 GA 소속 설계사 351명이 2,687건의 신규계약을 모집하며 3,502건의
기존계약을 부당하게 소멸시켰습니다.

**확인한 사실 / 가설 / 미확인**: 금융위 발표는 원문을 직접 확인했습니다. 금감원 수치는 언론
교차확인이며 원문은 찾지 못했습니다. 실제 GA 상담 데이터는 없어 15건의 가상(synthetic)
시나리오로 대체했습니다 — 이 사실을 결과물 전체에서 숨기지 않습니다.

## 2. 접근 방법

비교 방법 네 가지를 검토했습니다: (a) 사람 수기 확인, (b) 규칙기반 — 계약 비교의 자연어
다양성 때문에 기각, (c) 범용 모델에 그냥 물어보기(naive), (d) 스키마·Unknown/Conflict/Evidence
규칙을 강제한 구조화 파이프라인(structured). (c)와 (d)를 실제로 돌려서 비교했고, 나중에
(c)와 (d) 사이의 차이가 "스키마"와 "지침" 중 무엇 때문인지 분리하기 위해 세 번째 축(지침
동일+자유서술)도 추가했습니다.

사람이 직접 비교하던 과정을 AI가 먼저 수행하도록 만들고, 그 결과를 검증할 수 있도록
평가 기준과 역할 경계를 먼저 정의했습니다. AI의 역할은 Extract(찾기) → Compare(비교) →
Flag(표시) → Unknown(모름 표시) → Evidence(근거연결)까지로 한정하고, 최종 판단(가입/전환
여부)은 설계사의 몫으로 뒀습니다. 설계 단계에서 이 경계와 평가 규칙(매칭 M1~M3, 검증
A~E)을 먼저 고정한 뒤 구현으로 넘어갔습니다 — 자세한 결정 과정은
[`03_평가설계.md`](03_평가설계.md).

15개 테스트 케이스를 설계했지만(GT까지 전부 완성, [`02_테스트케이스.md`](02_테스트케이스.md)),
**실제 실행은 7개(TC-01, 03, 04, 05, 10, 11, 15)로 한정**했습니다 — 이유는 5번 참고.

## 3. 핵심 구현

```
src/llm_extractor.py                 구조화 파이프라인 실행(OpenAI function calling + jsonschema 검증)
src/review_app.py                    업무 효과 검토 UI(Streamlit) — AI 초안을 GT 없이 확인·수정
src/conflict_detector.py             GT·LLM 미사용 — 문서 원문만으로 conflict 독립 탐지
src/evaluate.py                      GT 대조 자동 평가기(B5 포함, 임의 파일 경로 지정 가능)
src/naive_baseline.py                naive baseline 실행
src/instructed_freeform_baseline.py  세 번째 baseline 실행
prompt/system_prompt.md              구조화 파이프라인 시스템 프롬프트
prompt/instructed_freeform_prompt.md 세 번째 baseline용(지침 동일 + 자유서술)
schema/output_schema.json            공통 Output JSON Schema
schema/validation_rules.md           자동 평가 규칙 A~E, 매칭 규칙 M1~M3
data/tc01~15_data.json               15개 테스트 케이스 원본(시나리오 + GT)
results/work_effect/                 검토 UI용 패킷, 실행 로그(app_logs/)
```

`05_업무효과검증_설계.md`에 이 검토 UI로 무엇을 측정할지(작업시간·중요 변경사항 누락·
잘못된 확정), 어떻게 편향을 줄일지 미리 정해뒀습니다 — 결과가 나오기 전에 해석 규칙부터
고정해두는 방식은 `03_평가설계.md`/`04_검증결과.md`와 같은 원칙입니다.

## 4. 주요 결과

**① 계약 차이 발견**: Naive 14/15, Structured 14/15 — 항목 단위로 다시 채점하면 두
방식의 발견 능력 자체는 차이가 없습니다. 처음엔 "naive와 structured의 단순 발견 능력
차이는 크지 않다"고 썼는데, 다시 보니 그 비교 자체가 naive는 케이스 단위 관대한 판정,
structured는 항목 단위 엄격한 비율이라 같은 기준이 아니었습니다. 같은 항목 단위로 맞춘
재계산 결과가 위 수치입니다(상세: `results/naive_vs_structured_recall_reanalysis.md`).

**② 검증 가능성**: 발견 능력과 별개로, 근거 명시·출처 충돌 보존·유불리 미판단(경계 준수)
세 축에서는 naive와 structured가 실제로 갈렸습니다.

| 기준 | Naive | Structured |
|---|---|---|
| 근거 명시 | 0/7 완전 충족 | Evidence 원문 존재율 100% (환각 0건) |
| 출처 충돌 보존 | **0/3 전부 실패** | 1/3 성공, 2/3 실패 — 단 evaluate.py의 D3로 **자동 검출됨** |
| 유불리 미판단(경계 준수) | 1/7만 준수 | 판단어 텍스트 스캔(E2) 0건 검출 |

**structured의 핵심 가치는 더 정확하다는 데 있지 않고, AI 결과를 검증하고 실패를 감지
가능한 데이터로 남길 수 있는 운영 구조에 있었습니다.** naive에서 확인된 오류는 원문을
다시 대조해야 발견할 수 있었던 반면, structured에서 확인된 오류는 평가기의 규칙을 통해
자동으로 검출할 수 있었습니다. 세 번째 baseline(지침 동일+자유서술)으로 나눠본 결과, 이
세 축의 차이는 상당 부분이 스키마가 아니라 명시적 지침 자체에서 왔습니다.

D3(GT 있는 평가환경)와 별개로, GT 없이 문서 원문만으로 conflict를 독립 탐지하는
`conflict_detector.py`도 검증했습니다 — Ground Truth가 없는 상황에서도 원문 기반으로
충돌을 탐지할 수 있다는 가능성을 확인했습니다.

**③ 평가기 신뢰성**: 평가기(`src/evaluate.py`) 자체도 검증하는 과정에서 9건의 버그를
찾아 수정했습니다. 이 작업을 먼저 하지 않았다면 AI 결과가 정말 정확한지 아닌지도 알 수
없었을 것입니다. **전체 검증 과정, 평가기 자체 버그 9건, 반복 검증 기록은
[`04_검증결과.md`](04_검증결과.md)에 있습니다.**

**④ 실제 업무 효과**: 정답 없는 새 계약에서도 도울 수 있는가 — 이게 이 프로젝트의 다음
질문이었습니다. `conflict_detector.py`의 탐지 신호를 사람이 실제로 확인·수정하는 화면
(`review_app.py`)을 만들어 7개 케이스 전부에 연결하고, 2026-09-18에 실험자 1인이
7케이스×2조건=14세션을 실제로 실행했습니다. **핵심 질문("AI가 먼저 초안을 만들고 사람이
검토하면 반복 작업이 줄어드는가")에는 긍정적인 결과가 나왔습니다 — 작업시간이 AI 보조
조건에서 평균 75.6% 짧았습니다.** 다만 이는 실험자 1인의 자기실험(N=7) 결과이며, "AI가
사람보다 계약 차이를 더 잘 찾는다"는 의미는 아닙니다.

**⑤ 사람 검토의 한계**: 검토 과정을 들여다보며 다음 과제도 발견했습니다.
conflict·발화불일치 3개 케이스 중 사람이 능동적으로 잡아낸 건 0건이었습니다. AI가
표시한 오류는 검토 과정에서 대부분 그대로 수용되었고, AI가 놓친 오류를 사람이 추가로
발견한 사례는 이번 실험에서 확인되지 않았습니다. 즉 "AI가 초안을 만들고 사람이
검토한다"는 구조 자체가 자동으로 안전성을 보장하지는 않았습니다 — "AI 결과를 사람이
검증 없이 수용하는 경향을 검토 화면에서 어떻게 줄일지"가 다음 개선 과제입니다. 이번
실험에서는 structured의 오류가 평가 규칙을 통해 자동 검출되는 구조를 확인했지만, 이것이
실제 설계사의 오류 예방으로 이어지는지는 아직 검증하지 않았습니다. 상세는
`05_업무효과검증_설계.md`, `results/work_effect/trial_log.md` 참고.

### 비용 및 실행 특성

structured 7건은 재시도까지 포함한 실측
합계로 43,244 토큰(케이스당 평균 약 6,178 토큰)이 들었습니다. 눈에 띄는 점은 **7건 전부
`attempts: 2`** — 1차 시도는 매번 실패하고 2차에서만 성공했다는 뜻인데, 왜 항상 정확히
한 번만 재시도가 필요했는지는 아직 원인을 분석하지 않은 채로 남겨둡니다. 세 번째
baseline(지침 동일+자유서술) 5건은 평균 2,783 토큰·5.27초로, 이쪽은 처리시간까지
실측했습니다(`results/instructed_freeform/*_meta.json`). structured 7건은 처리시간
계측 로직이 그 이후에 추가돼 이번 7건에는 남아있지 않습니다. naive baseline은 실행
시점에 콘솔에만 찍고 파일로 저장하지 않아 실측치가 전혀 남아있지 않았습니다 — 이번에
`naive_baseline.py`가 결과마다 `_meta.json`을 저장하도록 고쳐서, 다음 실행분부터는 같은
방식으로 실측할 수 있습니다(지금까지의 7건은 추정치조차 없이 미기록으로 둡니다).

**비즈니스 연결(1개만 고름)**: 계약 전환 **상담 전 점검 도구**입니다. 지금 파이프라인의
입출력이 이미 이 용도에 맞고, 이 프로젝트가 실제로 검증한 것과 정확히 일치합니다(상담 중
실시간 개입이나 사후 감사는 요구사항이 달라 이번 결과물로 주장하지 않습니다).

**이 프로젝트가 남기는 것**: 우선순위는 (1) 재현 가능한 평가 데이터(15개 GT + 실패 사례)
→ (2) 사람의 수정이 다음 검증에 반영되는 구조 → (3) 항목 연결 기준(스키마) → (4)
현업자가 검토한 오류 사례 순입니다. 모델이 변경되더라도 동일한 테스트셋과 평가 기준으로
새로운 결과를 비교할 수 있는 기반으로 활용할 수 있습니다. 즉 이 프로젝트가 실제로 남기는
것은 완성된 상품이 아니라, 다음 검증을 더 빠르게 만들어 주는 재현 가능한 평가 자산입니다.

## 5. 한계

**우선순위 판단**: 발견하는 대로 전부 고치지 않았습니다. 평가기 회귀 버그(B5, 999,999원
값 위조를 못 잡던 문제)를 최우선으로 뒀습니다 — 다른 항목은 "AI/평가기가 얼마나
잘하는가"지만 이건 "얼마나 정확히 측정하고 있는가" 자체의 구멍이었기 때문입니다.
반대로 D2 오탐(같은 문서를 인용하는 무관한 항목을 중복 확정)과 paraphrase 매칭은 발견
즉시 의도적으로 보류했습니다 — 모든 발견 사항을 즉시 수정하면 평가기 자체의 개선이
끝없이 이어질 수 있어, 측정 신뢰성에 직접 영향을 주는 B5를 우선 수정하고 나머지는
한계로 남겼습니다.

**남은 한계**
- 15개 중 7개만 실제 실행했습니다. 나머지 8개(TC-02,06,07,08,09,12,13,14)는 설계·GT까지
  완성했지만 아직 실행하지 않았습니다 — "15개를 평가했다"고 표현하지 않습니다. 그중
  "기존 계약 해지 불이익" 전담 케이스(TC-07, TC-13)가 둘 다 여기 포함되는데, 복합 케이스인
  TC-15에서 이 항목을 AI가 실제로 놓친 사례가 있어(`04_검증결과.md` §7) 우선순위가 낮지
  않습니다.
- Weighted Recall이 미구현입니다 — 공식과 GT 가중치 데이터는 준비돼 있으나 evaluate.py가
  읽지 않아, 지금까지 나온 모든 Recall 수치는 unweighted입니다.
- evaluate.py의 D2 오탐과 paraphrase 매칭이 미구현 상태입니다 — 위 우선순위 판단 참고.
- 세 번째 baseline 비교는 5건·1회 샘플링 결과라 일반화하지 않습니다.
- 실제 사용자(설계사) 검증은 하지 않았습니다 — 실험자 1인의 통제 자기실험(N=7,
  2026-09-18)으로 작업시간 단축(75.6%)은 실측했지만, 실제 설계사가 썼을 때도 같은
  효과가 나는지는 여전히 확인하지 못했습니다.

**다음 검증 단계(가정)**: 실제 계약비교 상담 데이터에 접근할 수 있다고
가정하면, 다음 순서로 진행합니다.

1일차: 비식별 계약비교 상담 사례 3~5건을 확보하고, 미실행 8개 케이스 중 어디부터 실행할지
결정합니다 — "기존 계약 해지 불이익" 전담인 TC-07·TC-13이 가장 먼저 검토할 후보입니다.
2일차: D2 버그를 수정하고 Weighted Recall을 실제로 구현합니다.
3일차: paraphrase 매칭 전략을 정합니다(임계값 있는 유사도 vs 사람이 직접 하는 이진 판정).
4일차: 실제 데이터로 다시 실행하고, 우선순위가 높은 미실행 케이스부터 추가합니다.
5일차: 결과를 공유하고, Human Review Queue를 누가 언제 확정할지 운영 방식을 정합니다.

이 계획을 실행하려면 실제 계약비교 상담 데이터에 대한 접근 권한과, 2024-09-23 금감원
검사결과 원문(fss.or.kr) 확보가 먼저 필요합니다.

## 6. 실행 방법

```bash
python -m venv .venv && .venv/Scripts/python -m pip install -r requirements.txt
cp .env.example .env   # OPENAI_API_KEY 채우기

.venv/Scripts/python src/llm_extractor.py TC-01                # 구조화 파이프라인
.venv/Scripts/python src/naive_baseline.py TC-01               # naive baseline
.venv/Scripts/python src/instructed_freeform_baseline.py TC-01 # 세 번째 baseline
.venv/Scripts/python src/evaluate.py TC-01                     # 구조화 결과 채점
.venv/Scripts/python src/conflict_detector.py TC-01            # GT 없는 conflict 탐지
```

전체 7건 재현, 원본/교정본/fixture 지정 평가 등 상세 실행법은 [`README.md`](README.md)
"실행 방법"에서 확인할 수 있습니다. 샘플 흐름: `data/tc01_data.json`(입력) →
`results/structured/TC-01.json`(구조화 출력) → `results/metadata/TC-01_eval.json`(채점).
