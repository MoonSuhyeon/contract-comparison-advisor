# 정리 — 직접 정의한 과제: 기존계약-신규상품 비교 AI 판단 지원

> 원본  30X 챌린지의 C6("고객의 목소리를 제품과 운영에 연결하라")과는 니즈/반대이유/긴급도
> 구조화가 아니라는 점에서 다르다. 이 과제는 **직접 정의한 과제**이며(챌린지 문서 05번 섹션의
> "직접 정의한 과제도 환영합니다" 조항에 따름), 원래 C6보다 이 문제를 선택한 근거는 아래 1번에
> 있다. 상세 근거는 [`01_문제정의.md`](01_문제정의.md) 참고.

## 1. 문제

기존 보험계약을 신규 상품으로 전환할 때, 보험료 외에 보장·지급조건·갱신·환급금 같은 "눈에 잘
안 띄는" 조건 변화를 놓치면 고객이 손해를 볼 수 있다. AI는 이 차이를 근거와 함께 빠짐없이 찾아
보여주는 역할까지만 하고, 최종 판단(가입/전환 여부)은 설계사가 한다.

**왜 지금 이 문제인가**: 금융위원회가 2025-01-22 공식 발표(원문 직접 확인, `01_문제정의.md`
§1)에서 GA(법인보험대리점)의 "기존계약-신규계약 중요사항 비교 미고지"를 반복되는 불완전판매
문제로 지목했다. 2024-09-23 금감원 검사 결과(언론 9곳 교차확인, 원문 미확인 — 숨기지 않음)에
따르면 5개 대형 GA 소속 설계사 351명이 2,687건의 신규계약을 모집하며 3,502건의 기존계약을
부당하게 소멸시켰다.

**확인한 사실 / 가설 / 미확인**: 금융위 발표는 원문 직접 확인. 금감원 수치는 언론 교차확인이며
원문은 못 찾음. 실제 /GA 상담 데이터는 없어 15건의 가상(synthetic) 시나리오로 대체 —
이 사실을 결과물 전체에서 숨기지 않는다.

## 2. 접근 방법

비교 방법 네 가지를 검토했다: (a) 사람 수기 확인, (b) 규칙기반 — 계약 비교의 자연어 다양성
때문에 기각, (c) 범용 모델에 그냥 물어보기(naive), (d) 스키마·Unknown/Conflict/Evidence
규칙을 강제한 구조화 파이프라인(structured). (c)와 (d)를 실제로 돌려서 비교했고, 나중에
(c)와 (d) 사이의 차이가 "스키마"와 "지침" 중 무엇 때문인지 분리하기 위해 세 번째 축(지침
동일+자유서술)도 추가했다.

AI의 역할은 Extract(찾기) → Compare(비교) → Flag(표시) → Unknown(모름 표시) →
Evidence(근거연결)까지로 한정하고, 최종 판단(가입/전환 여부)은 설계사의 몫으로 뒀다. 설계
단계에서 이 경계와 평가 규칙(매칭 M1~M3, 검증 A~E)을 먼저 고정한 뒤 구현으로 넘어갔다 —
자세한 결정 과정은 [`02_설계.md`](02_설계.md).

15개 테스트 케이스를 설계했으나(GT까지 전부 완성, [`03_테스트케이스.md`](03_테스트케이스.md)),
**실제 실행은 7개(TC-01, 03, 04, 05, 10, 11, 15)로 한정**했다 — 이유는 5번 참고.

## 3. 핵심 구현

```
prompt/system_prompt.md              구조화 파이프라인 시스템 프롬프트
prompt/instructed_freeform_prompt.md 세 번째 baseline용(지침 동일 + 자유서술)
schema/output_schema.json            공통 Output JSON Schema
schema/validation_rules.md           자동 평가 규칙 A~E, 매칭 규칙 M1~M3
src/llm_extractor.py                 구조화 파이프라인 실행(OpenAI function calling + jsonschema 검증)
src/naive_baseline.py                naive baseline 실행
src/instructed_freeform_baseline.py  세 번째 baseline 실행
src/evaluate.py                      GT 대조 자동 평가기(B5 포함, 임의 파일 경로 지정 가능)
src/conflict_detector.py             GT·LLM 미사용 — 문서 원문만으로 conflict 독립 탐지
data/tc01~15_data.json               15개 테스트 케이스 원본(시나리오 + GT)
```

## 4. 주요 결과

| 기준 | Naive | Structured |
|---|---|---|
| 주요 변경사항 발견 | 6/7 | Recall 0.75~1.0 (unweighted, 자동 채점) |
| 근거 명시 | 0/7 완전 충족 | Evidence 원문 존재율 100% (환각 0건) |
| 출처 충돌 보존 | **0/3 전부 실패** | 1/3 성공, 2/3 실패 — 단 evaluate.py의 D3로 **자동 검출됨** |
| 유불리 미판단(경계 준수) | 1/7만 준수 | 판단어 텍스트 스캔(E2) 0건 검출 |

**결론(과장하지 않음)**: naive와 structured의 단순 발견 능력 차이는 크지 않다. **structured의
핵심 가치는 매 순간 더 정확하다는 데 있지 않고, AI 결과를 검증하고 실패를 감지 가능한 데이터로
남길 수 있는 운영 구조에 있었다.** 세 번째 baseline(지침 동일+자유서술)으로 나눠본 결과, 이
차이의 상당 부분이 스키마가 아니라 명시적 지침 자체에서 왔다. 그리고 D3(GT 있는 평가환경)와
별개로, GT 없이 문서 원문만으로 conflict를 독립 탐지하는 `conflict_detector.py`도 검증했다 —
실제 신규 계약처럼 정답이 없는 상황에서도 같은 방식이 동작한다는 근거다. **전체 검증 과정,
평가기 자체 버그 9건, 반복 검증 기록은 [`04_검증결과.md`](04_검증결과.md)에 있다.**

**현업가치 추정(실사용자 테스트 아님)**: naive 7건 중 4건(57%)에서 설계사가 그대로 믿었다면
놓쳤을 정보가 있었던 반면, structured는 검출된 실패 100%가 D3로 자동 표시됐다 — "얼마나
정확한가"보다 "실패를 스스로 신고하는가"가 더 방어 가능한 지표다.

**비즈니스 연결(1개만 고름)**: 계약 전환 **상담 전 점검 도구**. 지금 파이프라인의 입출력이
이미 이 용도에 맞고, 이 프로젝트가 실제로 검증한 것과 정확히 일치한다(상담 중 실시간 개입이나
사후 감사는 요구사항이 달라 이번 결과물로 주장하지 않는다).

**기술적 해자**: 우선순위는 (1) 재현 가능한 평가 데이터(15개 GT + 실패 사례) → (2) 사람의
수정이 다음 검증에 반영되는 구조 → (3) 항목 연결 기준(스키마) → (4) 현업자가 검토한 오류
사례. (1)(2)는 범용 모델이 좋아져도 "조직이 무엇을 맞다고 확인했는지의 축적"이라 그대로
남는다.

## 5. 한계

**우선순위 판단**: 발견하는 대로 전부 고치지 않았다. 평가기 회귀 버그(B5, 999,999원 값 위조를
못 잡던 문제)를 최우선으로 뒀다 — 다른 항목은 "AI/평가기가 얼마나 잘하는가"지만 이건 "얼마나
정확히 측정하고 있는가" 자체의 구멍이었기 때문이다. 반대로 D2 오탐(같은 문서를 인용하는 무관한
항목을 중복 확정)과 paraphrase 매칭은 발견 즉시 의도적으로 보류했다 — 매번 발견하는 대로 다
고치면 "언제 끝나는지 모르는 평가기 다듬기"가 되어 이 프로젝트의 핵심 메시지(무엇을 실행했고
무엇을 안 했는지 명확히 긋는 것)를 흐리기 때문이다.

**남은 한계**
- 15개 중 7개만 실제 실행. 나머지 8개(TC-02,06,07,08,09,12,13,14)는 설계·GT까지 완성됐으나
  미실행 — "15개를 평가했다"고 표현하지 않는다.
- Weighted Recall이 미구현이다 — 공식과 GT 가중치 데이터는 준비돼 있으나 evaluate.py가 읽지
  않아, 지금까지 나온 모든 Recall 수치는 unweighted다.
- evaluate.py의 D2 오탐, paraphrase 매칭 미구현 — 위 우선순위 판단 참고.
- 세 번째 baseline 비교는 5건·1회 샘플링 결과라 일반화하지 않는다.
- 실제 사용자(설계사) 검증은 하지 않았다 — 현업가치는 추정치다.

**합류 후 5영업일 계획(가정)**
1일차: 실제(비식별) 계약비교 상담 사례 3~5건 확보 협의, 미실행 8개 케이스 실행 여부 결정
2일차: D2 버그 수정, Weighted Recall 실제 구현
3일차: paraphrase 매칭 전략 결정(임계값 있는 유사도 vs 사람 이진판정 방식)
4일차: 실제 데이터로 재실행, 우선순위 높은 미실행 케이스부터 추가
5일차: 결과 공유, Human Review Queue 운영 방식(누가 언제 확정하는지) 확정

**확인이 필요한 의존관계**: 실제 계약비교 상담 데이터 접근 권한, 2024-09-23 금감원 검사결과
원문(fss.or.kr) 확보.

## 6. 실행 방법

```bash
python -m venv .venv && .venv/Scripts/python -m pip install -r requirements.txt
cp .env.example .env   # OPENAI_API_KEY 채우기

.venv/Scripts/python src/llm_extractor.py TC-01                # 구조화 파이프라인
.venv/Scripts/python src/naive_baseline.py TC-01               # naive baseline
.venv/Scripts/python src/instructed_freeform_baseline.py TC-01 # 세 번째 baseline
.venv/Scripts/python src/evaluate.py TC-01                     # 구조화 결과 채점
.venv/Scripts/python src/conflict_detector.py TC-01            # GT 없는 conflict 탐지
```

전체 7건 재현, 원본/교정본/fixture 지정 평가 등 상세 실행법은 [`README.md`](README.md) "실행
방법" 참고. 샘플: `data/tc01_data.json`(입력) → `results/structured/TC-01.json`(구조화 출력)
→ `results/metadata/TC-01_eval.json`(채점).
