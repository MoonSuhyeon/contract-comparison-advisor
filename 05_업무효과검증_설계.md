# 업무 효과 검증 설계

이 프로젝트의 목적은 AI가 계약을 비교할 수 있다는 것을 보여주는 데서 끝나지 않습니다.
실제 설계사가 기존 계약과 신규 상품을 비교할 때 드는 반복 작업을 줄이고, 중요한 변경사항을
놓치지 않도록 돕는지를 확인해야 합니다.

현재까지의 검증에서는 AI가 계약 변경사항을 구조화하고, 근거·Unknown·Conflict를 함께
표시할 수 있는지와 그 결과를 자동으로 검증할 수 있는지를 확인했습니다.

하지만 이것만으로는 업무 효과를 증명할 수 없습니다. 실제 업무에서는 설계사가 AI 결과를
받아 확인하고 수정하는 과정까지 포함되어야 하기 때문입니다.

따라서 업무 효과 검증에서는 기존의 "원문 확인 → 직접 비교 → 변경사항 작성 → 근거 확인"
업무를 "AI 비교 초안 생성 → 변경사항 확인·수정 → 최종 결과 확정" 업무로 바꾸고, 두 방식의
차이를 측정합니다.

**단, AI가 "문제없음"이라고 판단했다고 해서 사람의 비교·고지를 생략하지 않습니다.** 최종
비교 결과 확인과 전환 여부 판단은 항상 사람이 담당합니다. 이는 `01_문제정의.md` §1이
문제 삼는 "부당승환계약"(설계사가 비교·고지 의무를 생략하는 것)을 AI가 대신 반복하지
않기 위한 전제입니다.

**아직 실행 전 단계이며, 이 문서에는 실제 측정값이 없습니다.** 실행 결과는 §5와
`04_검증결과.md`에 반영합니다.

## 1. 검증하려는 업무 변화

**기존 방식**: 설계사가 기존 계약과 신규 상품의 원문을 직접 확인하면서 비교표를 작성합니다.

```
기존 계약 원문 + 신규 상품 원문 + 상담 내용
                  │
          설계사가 직접 대조
                  │
             변경사항 작성
                  │
               근거 확인
                  │
          중요 변경사항 판단
                  │
               최종 확인
```

**AI 보조 방식**: AI가 먼저 비교 초안을 만들고, 설계사는 결과를 확인하고 수정합니다.

```
기존 계약 원문 + 신규 상품 원문 + 상담 내용
                  │
            AI 비교 초안 생성
                  │
   변경사항 / 근거 / Unknown / Conflict / 고객 니즈 관련 변화
                  │
             설계사 확인·수정
                  │
            최종 비교 결과 확정
                  │
               전환 여부 판단
```

핵심은 사람의 업무를 없애는 것이 아니라, **사람이 비교표를 처음부터 만드는 업무를 AI가
초안 단계까지 앞당기는 것**입니다.

## 2. AI가 실제 업무에서 담당하는 범위

`evaluate.py`의 모든 규칙을 실제 업무에 그대로 쓸 수 있는 건 아닙니다. 현재 평가기의 일부
규칙은 Ground Truth와 AI 결과를 비교해야 판정되므로, **개발 단계에서 AI의 성능을 평가하기
위한 하네스**입니다. 실제 업무에는 Ground Truth가 없으므로 역할을 나눠야 합니다.

**개발·검증 단계**: `evaluate.py`로 AI가 정답과 얼마나 일치하는지 평가합니다(Ground Truth
→ AI 결과 → `evaluate.py` → Recall/Precision/Evidence/Unknown/Conflict). `schema/validation_rules.md`의
B2~B4·C1~C2·D1~D4가 여기 해당합니다.

**실제 업무 단계**: Ground Truth 없이 원문 자체만으로 확인할 수 있는 항목만 품질 게이트로
씁니다(AI 결과 → 구조 검사 → Evidence 원문 존재 여부 확인 → 판단어 검사 → 원문 기반
Conflict 탐지 → 품질 문제가 있는 결과 표시 → 설계사 전체 결과 확인). `validation_rules.md`의
A(구조 검사)·B1(근거 원문 대조)·E(판단어 검사)와 `src/conflict_detector.py`가 여기 해당합니다.

즉 현재 평가기를 그대로 실시간 품질 게이트로 쓰는 게 아니라, **검증된 규칙 중 실제 입력
데이터만으로 확인 가능한 규칙만 별도의 업무용 검증 로직으로 다시 구현**해야 합니다. 이
구현은 아직 하지 않았고, §8에서 앞으로 할 일로 남겨둡니다.

## 3. 실제 업무 흐름 구현 (목표 설계)

업무 효과를 측정하려면 AI 결과를 실제 업무에서 쓸 수 있는 형태로 먼저 만들어야 합니다.
`results/structured/TC-XX.json`을 사람이 그대로 읽는 대신, 설계사가 확인·수정할 수 있는
검토 화면(또는 그에 준하는 워크시트)을 목표로 합니다. 아래는 그 목표를 보여주는 예시이며,
**아직 구현되지 않았습니다** — 실제 구현 항목은 §8 참고.

| 항목 | 기존 계약 | 신규 계약 | 변화 | 근거 | 상태 |
|---|---|---|---|---|---|
| 암 보장금액 | 3,000만원 | 2,000만원 | 축소 | 계약서 원문 | 확인 필요 |
| 보험료 | 95,000원 | 88,000원 | 감소 | 상품자료 | 확인 |
| 갱신기간 | 10년 | 5년 | 변경 | 계약서 원문 | 확인 필요 |
| 환급금 | 45% | 35% | 감소 | 상품자료 | 확인 필요 |

설계사는 각 항목에 대해 AI 결과 확인, 잘못된 값 수정, 누락된 변경사항 추가, 근거 원문
확인, Conflict/Unknown 확인, 최종 결과 확정을 수행합니다. AI 결과를 그대로 승인하는 게
아니라 **AI 초안을 업무 입력값으로 쓰고 사람이 최종 확정**하도록 합니다.

## 4. 비교할 두 가지 업무 방식

업무 효과는 동일한 케이스를 두 가지 방식으로 처리하면서 비교합니다.

**A. 기존 방식**: AI 결과를 제공하지 않습니다. 설계사는 기존 계약, 신규 상품, 상담 내용을
직접 확인해 비교 결과를 작성합니다.

**B. AI 보조 방식**: AI가 생성한 비교 초안을 제공합니다. 설계사는 AI 결과를 확인하고
필요한 부분을 수정한 뒤 최종 비교 결과를 확정합니다.

두 조건에서 최종 결과의 형식은 동일하게 맞춥니다(변경사항 각각의 기존 값/신규 값/변화
방향/근거, Unknown, Conflict, 고객 니즈 관련 중요 변경사항). 따라서 비교 대상은 "AI 결과가
보기 편한가"가 아니라, **같은 업무 결과를 만드는 데 필요한 사람의 작업량과 정확성이
달라지는가**입니다.

## 5. 업무 효과 측정 지표

### 5-1. 작업시간

각 케이스를 최종 비교 결과까지 완성하는 데 걸린 시간을 측정합니다. A 조건은 원문 열람
시작부터 비교 결과 작성 완료까지, B 조건은 AI 결과 열람 시작부터 검토·수정을 거쳐 최종
결과 작성 완료까지 측정합니다. 측정 결과는 조건별 평균 작업시간과 케이스별 작업시간으로
기록합니다.

### 5-2. 중요 변경사항 누락

각 조건에서 최종 작성한 결과를 Ground Truth와 비교해, 반드시 찾아야 하는 중요 변경사항이
최종 결과에 반영되지 않은 개수를 기록합니다(예: 기존 방식 4개 중 1개 누락, AI 보조 4개
중 0개 누락). 보장금액·보장범위 축소, 보험금 지급조건 변화, 갱신조건 변화, 해지환급금
변화, 면책·감액조건 변화, 고객 니즈와 연결되는 중요 변화의 누락 여부를 특히 확인합니다.
업무시간이 줄더라도 중요한 변경사항을 더 많이 놓친다면 업무 효과가 있다고 보지 않으므로
시간과 함께 측정합니다.

### 5-3. 잘못된 확정

AI 보조 방식에서는 AI의 결과를 사람이 그대로 받아들이는 과정에서 새로운 오류가 생길 수
있습니다. Conflict가 존재하는 케이스(TC-04, TC-05, TC-15)에서 AI가 충돌을 표시했는가,
설계사가 원문을 확인했는가, 최종적으로 올바른 상태로 확정했는가를 기록합니다. 이를 통해
AI가 사람의 판단을 대신하는 것이 아니라, 사람이 잘못 확정하지 않도록 필요한 확인 지점을
제공하는지를 확인합니다.

## 6. 실험 설계

현재 실제 실행 가능한 7개 케이스(TC-01, TC-03, TC-04, TC-05, TC-10, TC-11, TC-15)를
대상으로 먼저 검증합니다. 각 케이스는 기존 방식(A)과 AI 보조 방식(B)으로 모두 처리합니다.

동일 케이스를 연속해서 처리하면 앞선 경험을 기억하는 연습 효과가 생길 수 있으므로, 조건의
선후 순서를 사전에 정합니다. 배정은 `random.seed(20260917)`로 고정합니다.

| 케이스 | 1차 | 2차 |
|---|---|---|
| TC-01 | B | A |
| TC-03 | A | B |
| TC-04 | B | A |
| TC-05 | A | B |
| TC-10 | A | B |
| TC-11 | B | A |
| TC-15 | A | B |

1차 세션을 끝낸 뒤 최소 하루의 간격을 두고 2차 세션을 진행합니다. 실험 중에는 Ground
Truth를 확인하지 않고, 두 조건의 작업을 모두 끝낸 뒤에만 대조합니다.

## 7. 결과 해석

업무 효과는 단일 숫자로 판단하지 않습니다. 작업시간·중요 변경사항 누락·잘못된 확정
세 가지를 함께 봅니다.

| 경우 | 결과 패턴 | 해석 |
|---|---|---|
| 1 | 작업시간 감소 + 누락 감소·유지 + 오확정 증가 없음 | AI 보조 방식이 더 적은 작업으로 같은 수준 이상의 결과를 만드는 데 도움이 됐다 |
| 2 | 작업시간 감소 + 누락 증가 | 단순한 생산성 향상으로 해석하지 않는다 — AI 결과의 검증 방식이나 화면을 개선해야 한다 |
| 3 | 작업시간 차이 없음 + 누락 감소 | 시간 절감 효과는 확인되지 않았지만, 중요 변경사항을 확인하는 보조 수단으로서의 가치는 추가로 검토할 수 있다 |
| 4 | 작업시간 증가 + 누락 감소 | AI가 제공한 Conflict·Unknown을 확인하는 데 시간이 더 든 것인지, 검토 비용이 늘어난 것인지 별도로 본다 |

이번 검증은 실제 보험사·GA 업무 환경에서 수행하는 현업 검증이 아닙니다. 따라서 결과가
좋게 나오더라도 "합성 데이터 7개 케이스를 대상으로 한 통제된 자기실험에서 AI 보조 방식의
작업시간과 누락률 변화를 확인했다"로만 표현하고, "AI 도입으로 설계사 업무시간을 X% 단축할
수 있다"처럼 일반화하지 않습니다.

## 8. 추가 구현 사항

업무 효과 검증을 실행하려면 다음을 추가로 구현해야 합니다. 아직 하나도 만들지 않았습니다.

- **비교 결과 검토 화면(또는 워크시트)**: §3의 목표 설계를 실제로 채울 수 있는 형태로 구현
- **검토 상태 기록**: 각 항목의 "AI 제안 → 확인 → 수정 → 최종 확정" 상태 추적
- **근거 바로가기**: AI가 제시한 evidence를 원문에서 바로 확인할 수 있는 연결
- **Conflict/Unknown 구분 표시**: 일반 변경사항과 구분해 반드시 확인해야 하는 항목 표시
- **작업 로그 자동 기록**: `case_id`, `condition`, `start_time`, `end_time`, `duration`,
  `ai_suggestion_count`, `human_edit_count`(AI 원본 값을 수정한 횟수),
  `human_added_count`(AI가 놓친 항목을 사람이 새로 추가한 횟수), `evidence_recheck_count`,
  `conflict_review_count`, `unknown_review_count`, `final_change_count`,
  `final_omission_count`, `incorrect_confirmation_count`를 사람이 나중에 기억해 적는 게
  아니라 사용 과정에서 자동으로 저장. `human_edit_count`와 `human_added_count`를 분리하는
  이유는, 조건 A(처음부터 작성)와 조건 B(AI 초안 수정)에서 "같은 최종 결과를 만드는 데
  필요했던 작업의 성격"이 다르기 때문입니다 — B에서 사람이 AI 결과를 그대로 확인만 했는지,
  값을 고쳤는지, 아예 새로 추가했는지를 구분해야 "AI 초안이 실제로 얼마나 쓸만했는지"를
  말할 수 있습니다.
- **GT-불필요 품질 게이트**: §2에서 구분한 대로, `validation_rules.md`의 A·B1·E 규칙과
  `conflict_detector.py`만 뽑아 Ground Truth 없이도 새 계약 쌍에 돌아가는 별도 모듈로
  재구현(현재 `evaluate.py`는 이 용도로 쓸 수 없음)

이 중 검토 화면·작업 로그·품질 게이트가 갖춰져야 §5~§7의 측정이 실제로 가능합니다. 지금
`results/work_effect/packets/`와 `trial_log.md`는 이 화면·로그의 최소 버전(마크다운
워크시트 + 수기 기록)이며, 위 목록은 이걸 얼마나 자동화·구조화할지의 목표입니다.

## 9. 최종 목표

이 검증의 목표는 "AI가 사람보다 잘한다"를 증명하는 것이 아닙니다. 다음 질문에 답하는
것입니다.

**설계사가 계약을 처음부터 직접 비교하는 대신, AI가 만든 근거 있는 비교 초안을 검토하는
방식으로 업무를 바꾸었을 때, 중요한 변경사항을 놓치지 않으면서 반복 작업을 줄일 수
있는가?**

이를 확인하기 위해 "AI 비교 → 근거·Unknown·Conflict 표시 → 설계사 검토 → 수정 → 최종
비교 결과 → 업무시간·누락·오확정 측정"이라는 하나의 업무 흐름을 구현하고 검증합니다.
현재 단계는 AI의 비교 정확도 검증(`03_평가설계.md`, `04_검증결과.md`)까지 끝났고, 다음
단계는 AI 비교 결과를 사용하는 실제 업무 흐름 구현(§8)이며, 그 이후에야 이 문서의 업무
효과 측정(§4~§7)을 실행할 수 있습니다.

## 10. 재현

조건 배정은 다음 스크립트로 재현할 수 있습니다.

```python
import random
random.seed(20260917)
tcs = ['TC-01', 'TC-03', 'TC-04', 'TC-05', 'TC-10', 'TC-11', 'TC-15']
conditions = (['A', 'B'] * 4)[:7]
random.shuffle(conditions)
print(list(zip(tcs, conditions)))
```

업무 효과 검증용 패킷은 `data/tcXX_data.json`과 `results/structured/TC-XX.json`을
기반으로 만들며 Ground Truth는 패킷에 포함하지 않습니다. 실험 기록은
`results/work_effect/trial_log.md`에 남깁니다.
