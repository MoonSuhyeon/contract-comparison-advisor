# 업무 효과 검증 설계

`04_검증결과.md` §7은 이 프로젝트가 아직 확인하지 못한 것 중 하나로 "실제 사용자 검증
미실시"를 남겨뒀습니다. 설계사가 실제로 이 도구를 썼을 때 시간이 얼마나 줄고, 놓치는 항목이
얼마나 줄어드는지는 그동안 가상의 추정치로만 남아 있었습니다. 이 문서는 그 구멍을 메우기
위한 실험을 설계합니다. **아직 실행 전 단계이며, 이 문서에는 실제 측정치가 하나도 없습니다.**
실행 후 결과는 이 문서 §6과 `04_검증결과.md`에 반영합니다.

## 1. 왜 그냥 시간을 재면 안 되는가

가장 단순한 방법은 "기존 방식으로 비교 vs AI 결과 보고 비교"의 완료 시간을 재는 것입니다.
그런데 이 프로젝트에서는 그 방법을 곧바로 쓸 수 없는 사정이 있습니다.

**시간을 잴 사람이 이 테스트 케이스 세트를 직접 만든 사람입니다.** 15개 케이스의 Ground
Truth를 이미 알고 있고, 각 케이스가 어떤 실패를 겨냥해 설계됐는지도 알고 있습니다. 이 상태로
"기존 방식 8분 vs AI 보조 4분, 42% 단축" 같은 숫자를 낸다면, 그건 도구의 효과가 아니라
**정답을 아는 사람이 빨리 찾은 것**일 뿐입니다. 이 문제를 밝히지 않고 숫자만 내놓으면
`04_검증결과.md`가 지금까지 지켜온 태도(§8 결론에서 "아직 확인하지 못한 것"을 숫자 뒤에
숨기지 않은 것)와 어긋납니다.

정석은 Ground Truth를 모르는 제3자를 구해 블라인드로 진행하는 것이지만, 지금은 그럴 여건이
안 됩니다. 그래서 이 문서는 **제3자 블라인드 대신, 실험자 편향을 완전히 없애지는 못해도 최대한
줄이는 절차**를 설계합니다. 그리고 결과가 나오면 "포트폴리오용 통제 실험"으로만 쓰고 실제
업무 생산성으로 일반화하지 않기로, 결과를 보기 전에 미리 정해둡니다 — 이건 나중에 숫자가
좋게 나오든 나쁘게 나오든 바꾸지 않는 규칙입니다.

## 2. 편향 완화 설계

### 2-1. Counterbalanced crossover

같은 케이스를 조건 A(원문만)와 조건 B(AI 결과 제공) 양쪽 다 풀되, 먼저 푸는 조건을 케이스마다
미리 무작위로 고정합니다. 그렇게 하지 않으면 "두 번째로 푸는 조건이 첫 번째 기억 덕분에 항상
빠르게 나오는" 연습 효과(practice effect)가 특정 조건(대개 B) 쪽에만 쏠리게 됩니다.

배정은 이 문서를 쓰는 시점에 `random.seed(20260917)`로 한 번만 뽑았고, 실행 중에는 순서를
바꾸지 않습니다. 도중에 "이 케이스는 B를 먼저 하는 게 나을 것 같으니 바꾸자"처럼 판단이
섞이면, 그 판단 자체가 또 다른 편향이 되기 때문입니다.

| TC | 1차 조건 | 2차 조건 |
|---|---|---|
| TC-01 | B (AI 결과) | A (원문만) |
| TC-03 | A (원문만) | B (AI 결과) |
| TC-04 | B (AI 결과) | A (원문만) |
| TC-05 | A (원문만) | B (AI 결과) |
| TC-10 | A (원문만) | B (AI 결과) |
| TC-11 | B (AI 결과) | A (원문만) |
| TC-15 | A (원문만) | B (AI 결과) |

7개라 정확히 반반은 안 되지만(A 먼저 4건, B 먼저 3건), 최대한 균형을 맞췄습니다.

### 2-2. 세션 분리(쿨다운)

세션 1에서는 위 표의 "1차 조건"만 7건 전부 순서대로 진행합니다. 세션 2는 최소 하루 이상
지난 뒤 "2차 조건" 7건을 진행합니다. 같은 날 바로 이어서 하면 방금 본 내용이 너무 생생해서
두 번째 조건이 부당하게 빨라지기 때문입니다.

### 2-3. 그래도 남는 한계

이 두 가지 장치로도 지워지지 않는 한계를 미리 적어둡니다.

- 세션 2는 세션 1 때 이미 한 번 풀어본 케이스라, 완전히 처음 보는 케이스는 아닙니다. 이건
  크로스오버 설계 자체의 한계이고, 2-1의 균형 배정은 조건 간에 이 효과를 상쇄해줄 뿐 없애지는
  못합니다.
- 실험자가 원래 Ground Truth와 설계 의도를 알고 있다는 점은 어떤 절차로도 완전히 지워지지
  않습니다. §6에서 결과를 옮겨 적을 때, 그리고 이 문서를 다른 곳에서 인용할 때마다 이 한계를
  같이 적습니다.
- N=7입니다. 통계적으로 유의미한 차이를 주장하기엔 표본이 작습니다.

## 3. 측정 지표

**작업시간**: 조건 A는 `results/work_effect/packets/TC-XX_condition_A.md`를 열람한 시점부터
표의 작성란(old/new/변화방향/근거)을 다 채우고 불리한 변화·Unknown 항목까지 확정한 시점까지
잽니다. 조건 B는 `TC-XX_condition_B.md`를 열람한 시점부터 AI 결과를 검토해 작성란(수용/수정/
최종 old→new)을 다 채운 시점까지 잽니다. 스톱워치로 실측하고, 중간에 다른 일로 끊기면 그
구간은 빼고 순수 작업 시간만 남깁니다(끊긴 시각과 재개 시각은 로그에 메모).

**중요 변경사항 누락**: 각 케이스의 두 조건을 모두 마친 뒤, `data/tcXX_data.json`의
`ground_truth.changes`와 대조해 조건별로 작성란에 최종 반영하지 못한 항목 개수를 셉니다.

**근거 확인 시간**: 시간을 따로 재는 대신, AI가 evidence를 이미 줬는데도 원문을 다시 열어
대조한 횟수로 근사합니다. 조건 B 패킷 하단의 "AI 결과를 의심해서 원문을 다시 확인한 횟수"에
실시간으로 기록하고, 무엇을 확인했는지도 한 줄씩 남깁니다. 조건 A는 애초에 원문만 보므로 이
지표가 없습니다 — 대신 조건 A의 총 작업시간 자체가 근거를 찾는 시간을 이미 포함하고 있어서,
작업시간과 겹치지 않게 별도 지표로만 씁니다.

**잘못된 확정 방지**: conflict가 있는 케이스(TC-04, TC-05, TC-15)에서 조건 B를 진행하며 AI
결과를 그대로 받아들였는지, 의심해서 원문을 대조했는지, 최종적으로 맞게 확정했는지를 근거
재조회 로그와 함께 기록합니다. 특히 TC-04("암 보장은 그대로예요"라는 발화와 달리 실제로는
3,000만→2,000만원으로 축소된 경우)처럼 발화를 그대로 믿으면 오확정하는 케이스에서, AI가 낸
conflict 표시를 사람이 무시하지 않고 받아들였는지가 핵심입니다.

## 4. 실행 절차

1. 세션 1을 시작하기 전 `results/work_effect/trial_log.md`를 열어둡니다.
2. 2-1 표의 "1차 조건" 순서대로 TC-01부터 TC-15까지, 각 케이스에 해당하는 조건 패킷 파일을
   열고 스톱워치를 시작합니다.
3. 완료하면 종료 시각을 기록하고, `trial_log.md`에 소요시간·재조회 횟수를 적습니다(세션
   1에서는 누락 여부를 아직 판정하지 않습니다 — Ground Truth 대조는 세션 2까지 끝난 뒤로
   미룹니다).
4. 7건이 끝나면 세션 1을 종료하고, 최소 하루 이상 간격을 둡니다.
5. 세션 2에서 "2차 조건" 순서대로 같은 방식으로 진행합니다.
6. 세션 2까지 끝나면 케이스별로 Ground Truth와 대조해 누락 개수를 채웁니다.
7. `trial_log.md`의 집계 표를 채우고, 이 문서 §6에 결과를 옮겨 적습니다.

패킷 파일은 `results/work_effect/packets/TC-XX_condition_A.md`,
`TC-XX_condition_B.md`(7개 케이스 × 2조건, 총 14개)이며, `data/tcXX_data.json`과
`results/structured/TC-XX.json`에서 자동 생성했습니다. Ground Truth는 패킷에 포함하지
않았습니다. 로그 템플릿은 `results/work_effect/trial_log.md`입니다.

## 5. 집계·해석 규칙

결과를 보기 전에 다음 세 가지를 미리 정해둡니다.

"N% 감소"처럼 요약 수치를 쓸 때는 조건 A 평균과 조건 B 평균으로만 냅니다. 개별 케이스의
최댓값·최솟값으로 과장하지 않습니다.

이 수치를 인용할 때는 반드시 "실험자 1인 자기실험(Ground Truth를 아는 상태)", "N=7,
일반화하지 않음" 두 문장을 함께 적습니다. 이 문구 없이 "AI 도입 시 업무시간 42% 단축" 같은
형태로는 쓰지 않습니다.

누락과 오확정 방지는 시간과 달리 "몇 건 중 몇 건"으로만 표기하고 비율로 확대 해석하지
않습니다. N=7이라 비율 자체가 케이스 한두 개만 바뀌어도 크게 흔들리기 때문입니다.

## 6. 결과

아직 비어 있습니다. 세션 1·2를 마치고 `trial_log.md`를 채운 뒤 이 절을 채웁니다.

## 7. 재현

2-1의 배정표는 다음 스크립트로 재현할 수 있습니다(순서를 바꾸려는 목적이 아니라 검증용입니다).

```python
import random
random.seed(20260917)
tcs = ['TC-01', 'TC-03', 'TC-04', 'TC-05', 'TC-10', 'TC-11', 'TC-15']
conditions = (['A', 'B'] * 4)[:7]
random.shuffle(conditions)
print(list(zip(tcs, conditions)))
```

패킷 생성 스크립트는 `data/tcXX_data.json`과 `results/structured/TC-XX.json`을 읽어
`results/work_effect/packets/`에 조건별 마크다운을 만듭니다(Ground Truth 필드는 제외합니다).
