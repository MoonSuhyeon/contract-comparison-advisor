# 제출 요약 — 직접 정의한 과제: 기존계약-신규상품 비교 AI 판단 지원

> 원본 인톡 30X 챌린지의 C6("고객의 목소리를 제품과 운영에 연결하라")과는 니즈/반대이유/긴급도
> 구조화가 아니라는 점에서 다르다. 이 과제는 **직접 정의한 과제**이며(챌린지 문서 05번 섹션의
> "직접 정의한 과제도 환영합니다" 조항에 따름), 원래 C6보다 이 문제를 선택한 근거는 아래 1번에
> 있다.

## 1. 문제와 판단 요약

**문제**: 기존 보험계약을 신규 상품으로 전환할 때, 보험료 외에 보장·지급조건·갱신·환급금 같은
"눈에 잘 안 띄는" 조건 변화를 놓치면 고객이 손해를 볼 수 있다. AI는 이 차이를 근거와 함께
빠짐없이 찾아 보여주는 역할까지만 하고, 최종 판단(가입/전환 여부)은 설계사가 한다.

**왜 지금 이 문제인가**: 금융위원회가 2025-01-22 공식 발표(원문 직접 확인, `C6_문제정의_최종.md`
§4.2)에서 GA(법인보험대리점)의 "기존계약-신규계약 중요사항 비교 미고지"를 반복되는 불완전판매
문제로 지목하고 제도 개선(운영위험 평가제도 등)까지 내놓았다. 2024-09-23 금감원 검사 결과(언론
9곳 교차확인, 원문 미확인 — 이 사실을 숨기지 않음)에 따르면 5개 대형 GA 소속 설계사 351명이
2,687건의 신규계약을 모집하며 3,502건의 기존계약을 부당하게 소멸시켰다.

**확인한 사실 / 가설 / 미확인**: 금융위 발표는 원문 직접 확인. 금감원 수치는 언론 교차확인이며
원문은 못 찾음(제출 전 재확인 권장 사항으로 남김). 실제 인톡/GA 상담 데이터는 없어 15건의
가상(synthetic) 시나리오로 대체 — 이 사실을 결과물 전체에서 숨기지 않는다.

**비교한 방법**: (a) 사람 수기 확인, (b) 규칙기반 — 계약 비교의 자연어 다양성 때문에 기각,
(c) 범용 모델에 그냥 물어보기(naive), (d) 스키마·Unknown/Conflict/Evidence 규칙을 강제한
구조화 파이프라인(structured). (c)와 (d)를 실제로 돌려서 비교했다 — 3번 참고.

**스코프**: 15개 테스트 케이스를 설계했으나(GT까지 전부 완성), **실제 실행은 7개(TC-01, 03,
04, 05, 10, 11, 15)로 한정**했다. 이유는 4번 참고.

## 2. 실행 가능한 결과물

```
prompt/system_prompt.md         구조화 파이프라인의 시스템 프롬프트
schema/output_schema.json       공통 Output JSON Schema (changes/unchanged_items/unknowns/
                                 conflicts/additional_conditions/customer_needs/judgment)
schema/validation_rules.md      자동 평가 규칙 A~E, 매칭 규칙 M1~M3 (구현의 근거 문서)
src/llm_extractor.py            구조화 파이프라인 실행 (OpenAI function calling + jsonschema 검증 + 재시도)
src/naive_baseline.py           naive baseline 실행 (자유 텍스트)
src/evaluate.py                 GT 대조 자동 평가기 (Recall/Precision/Evidence/Unknown/Conflict/Boundary)
data/tc01~15_data.json          15개 테스트 케이스 원본 (시나리오 + GT)
results/structured/             구조화 파이프라인 실행 결과 7건
results/baseline/                naive baseline 실행 결과 7건(자유 텍스트)
results/metadata/                evaluate.py 채점 결과 JSON
results/corrected/                Human Review로 직접 수정한 사례(TC-05)
```

**실행 방법**
```bash
python -m venv .venv && .venv/Scripts/python -m pip install -r requirements.txt
cp .env.example .env   # OPENAI_API_KEY 채우기
.venv/Scripts/python src/llm_extractor.py TC-01      # 구조화 파이프라인
.venv/Scripts/python src/naive_baseline.py TC-01     # naive baseline
.venv/Scripts/python src/evaluate.py TC-01           # 구조화 결과 채점
```

**샘플**: `data/tc01_data.json`(입력) → `results/structured/TC-01.json`(구조화 출력) /
`results/baseline/TC-01.txt`(naive 출력) → `results/metadata/TC-01_eval.json`(채점).

## 3. 비교 기준과 검증 결과 (실측)

| 기준 | Naive | Structured |
|---|---|---|
| 주요 변경사항 발견 | 6/7 | Recall 0.75~1.0 (자동 채점) |
| 근거 명시 | 0/7 완전 충족 | Evidence 원문 존재율 100% (자동 채점, 환각 0건) |
| 출처 충돌 보존 | **0/3 전부 실패** | 1/3 성공(TC-04), 2/3 실패(TC-05, TC-15) — 단, 실패가 evaluate.py의 D3로 **자동 검출됨** |
| 유불리 미판단(경계 준수) | 1/7만 준수 | 판단어 텍스트 스캔(E2) 결과 0건 검출 |

전체 데이터는 `results/naive_baseline_human_review.md`(naive, 사람이 7건 직접 대조) /
`results/metadata/*.json`(structured, 자동 채점) 참고.

**결론(과장하지 않음)**: naive와 structured의 단순 변경사항 발견 능력 차이는 크지 않다(6/7 vs
Recall 대부분 0.75 이상). Structured도 conflict를 naive와 마찬가지로 놓쳤다(TC-05, TC-15).
**이번 실험에서 확인한 structured의 핵심 가치는 매 순간 더 정확하다는 데 있지 않고, AI 결과를
검증하고 실패를 감지 가능한 데이터로 남길 수 있는 운영 구조에 있었다.** naive의 실패는 사람이
7건을 전부 원문과 대조해야만 드러났지만, structured의 실패는 evaluate.py가 자동으로 D3
AUTO-FAIL로 표시해 곧바로 Human Review Queue로 넘어갔다(4번 참고, 실제 시연함).

## 3-1. 정답(GT) 없는 상황에서의 탐지

위 3번의 D3는 **GT(정답)가 이미 있는 평가 환경**에서 "AI가 GT에 적힌 conflict를 놓쳤는가"를
사후 대조하는 것이다. 실제 신규 계약에는 미리 작성된 정답이 없으므로, "정답이 있는 평가
환경에서 충돌 누락을 자동 검출했다"는 것과 "정답 없이도 확인 필요 신호를 스스로 만들 수
있다"는 것은 서로 다른 주장이다. 후자를 별도로 검증했다.

`src/conflict_detector.py`는 GT도 LLM도 쓰지 않는다. 신규 상품의 서로 다른 공식 문서
(`new_product.documents`) 원문에서 %/원 수치를 정규식으로 뽑고, 문장 단위 한글 2-gram
유사도로 "같은 항목·다른 값"을 찾는다. TC-05(40% vs 35%)에 실행한 결과:

**원문 확인 → 차이·충돌 표시 → 사람 확인/수정 → 기록**
1. 원문 확인: `product_summary_brochure`("10년 시점 해지환급률 약 40%")와
   `product_terms_detail`("...35%로 한다")을 정규식으로 각각 독립 추출
2. 충돌 표시: `results/conflict_detection/TC-05.json`에 후보로 저장(GT 미참조 상태)
3. 사람 확인: 같은 파일의 `_human_review`에서 GT와 대조해 true_positive 확인(이 검증
   단계에서만 GT 사용 — 탐지 자체는 GT 없이 이뤄짐)
4. 기록: `results/conflict_detector_log.md`에 N=7 전체 실행 결과, 오탐 0건 확인 포함

| 자동으로 잡히는 범위 | 여전히 놓치는 범위 |
|---|---|
| 신규 상품 내 서로 다른 공식 문서가 같은 항목에 다른 수치를 제시하는 경우(문서 대 문서 충돌) | 설계사 발화와 데이터의 불일치(TC-04/15의 source_discrepancy — 발화는 애초에 "공식 문서"가 아니라 이 탐지기의 비교 대상이 아님) |
| | 자기모순(TC-15의 unchanged 위장 — 수치 충돌이 아니라 논리적 모순) |
| | 비수치 항목(면책조항 문구 차이 등) |
| | paraphrase(표현은 다르지만 의미가 같은 경우) |

## 4. AI 활용·수정 기록

- **모델**: OpenAI `gpt-4o-mini` (구조화 파이프라인 + naive baseline 동일 모델 사용 — 모델 차이가
  아니라 프롬프트·스키마·검증 구조의 차이만 비교되도록 통제)
- **토큰 사용량(기록된 것)**: 구조화 파이프라인 8회 호출(TC-01은 v1+v2 두 번) 합계 약 48,500
  토큰. TC-05의 최초 스키마 검증 실패 시도(3회, 이후 코드 수정 계기가 됨)는 크래시로 사용량이
  기록되지 않음 — 정직하게 밝힌다. naive baseline은 사용량 기록 로직을 넣지 않아 미측정(알려진
  한계).
- **비용**: gpt-4o-mini 공개 요금 기준(시점에 따라 달라질 수 있어 단정하지 않음) 전체 1센트
  내외로 추정.
- **직접 판단·수정한 것**:
  - TC-01 v1에서 배열 라우팅 오류(값은 맞는데 배치가 틀림)를 발견 → 필드명 `change`→`delta_type`
    변경 + 프롬프트에 명시 규칙과 5단계 자체검사 추가 → v2로 3/4 개선, 잔여 1건은 가설만 남기고
    더 파고들지 않기로 결정(`results/tc01_iteration_log.md`)
  - evaluate.py 자체의 버그 7건을 실행하며 발견·수정(인코딩 크래시, 환각/출처오귀속 혼동,
    매칭 실패로 Recall 0 되는 문제, conflict 매칭 키 버그, Recall 150% 되는 중복매칭 버그 등) —
    `results/evaluator_validation_log.md`에 케이스별로 전부 기록
  - TC-05의 conflict 누락을 사람이 직접 확인·수정(Human Review 시연) → 수정 후 재검증에서
    예상과 다른 새로운 평가기 한계(D2 오탐)를 추가로 발견 → 이번엔 고치지 않고 기록만 남김
  - paraphrase 퍼지 매칭은 오탐 위험과 임계값 근거 부족으로 **의도적으로 구현하지 않음**(평가
    한계로 명시)
  - 추가 회귀 테스트로 교정본의 premium 값을 90,000→999,999로 의도적으로 위조하고 evidence
    문장은 그대로 둔 뒤 evaluate.py를 돌려봄 → 걸러지지 않는 것을 확인 → "evidence 문장이
    원문에 있다"와 "선언한 값이 그 문장과 일치한다"가 다른 검사임을 확인하고 **B5(값-근거
    일치)** 신설, fixture 3종으로 수정 전/후 대조(`results/evaluator_validation_log.md` Phase 8)
  - 그 과정에서 `results/metadata/TC-05_eval.json`이 실제로는 교정본 평가 결과였던(원복 누락)
    진짜 재현성 버그를 발견·수정 — `evaluate.py`가 임의 파일 경로를 받도록 고쳐 원본/교정본/
    fixture 평가 결과 파일을 명확히 분리
  - `src/conflict_detector.py` 신설 — GT 없이 문서 원문만으로 conflict를 독립 탐지(3-1 참고)

## 5. 실패와 인계

**남은 한계**
- 15개 중 7개만 실제 실행. 나머지 8개(TC-02,06,07,08,09,12,13,14)는 설계·GT까지 완성됐으나
  미실행 — "15개를 평가했다"고 표현하지 않는다.
- evaluate.py의 알려진 잔여 버그: D2가 "같은 문서를 인용하는 무관한 change"와 "진짜 중복 확정"을
  구분 못함(TC-05 Human Review 검증 중 발견). Weighted Recall, 다중 축 동시 감점 여부는 코드에
  아직 명시적으로 정의되지 않음.
- paraphrase(표현은 다르지만 의미가 같은 경우) 매칭 실패는 미해결 — TC-15의 `coverage_scope_cancer`
  가 실측 사례.
- naive baseline의 토큰/비용 미측정.

**합류 후 5영업일 계획(가정)**
1일차: 실제(비식별) 계약비교 상담 사례 3~5건 확보 협의, TC-02/06~09/12~14 실행 여부 결정
2일차: D2 버그 수정, Weighted Recall·다중축 감점 규칙 코드화
3일차: paraphrase 매칭 전략 결정(임계값 있는 유사도 vs 사람 이진판정 방식)
4일차: 실제 데이터로 재실행, 8개 미실행 케이스 중 우선순위 높은 것부터 추가
5일차: 결과 공유, Human Review Queue 운영 방식(누가 언제 확정하는지) 확정

**확인이 필요한 의존관계**: 실제 계약비교 상담 데이터 접근 권한, 2024-09-23 금감원 검사결과
원문(fss.or.kr) 확보.

## 6. 추가로 확인한 것과 우선순위 판단

이 프로젝트의 철학(실제로 돌려보고 실패를 찾아 기록한다)을 계속 유지하면서 아래 순서로
우선순위를 정해 점검을 이어갔다. 전부를 똑같이 다듬는 대신 무엇이 결과의 신뢰성에 더 큰
영향을 주는지로 순서를 정했다.

**우선 처리한 것**
1. **평가기 자체의 회귀 버그(B5)** — 최우선. 다른 항목은 전부 "AI/평가기가 얼마나 잘하는가"의
   문제지만, 이건 "얼마나 정확히 측정하고 있는가" 자체의 구멍이었다. 측정이 틀리면 나머지 결과
   전부가 의심받는다고 판단해서 가장 먼저 고쳤다.
2. **GT 없는 검토 루프 문서화** — `conflict_detector.py`가 이미 있었으므로, 새로 만들기보다
   "D3(GT 있음)와 이 탐지기(GT 없음)는 다른 층위"라는 구분을 제출 문서 전면에 명시적으로
   드러내는 것을 우선했다.
3. **`C6_문제정의_최종.md` 누락 발견·복구** — 로컬 Downloads 폴더에만 있고 프로젝트 폴더로
   옮겨진 적이 없어 커밋된 적이 없었던 것을 확인, 원본 그대로 추가함.

**추가로 시도한 것**
- **세 번째 baseline(구조화 지침 + 자유서술)**: naive/structured의 차이에 "지침 상세도"와
  "출력 구조 강제"가 각각 얼마나 기여했는지 분리하기 위해 실행함. 프롬프트를 실제로 튜닝하는 데
  쓰이지 않은 held-out 5건(TC-03,05,10,11,15)으로 실행·평가. **결론: naive의 실패 상당 부분은
  스키마가 아니라 지침 부재 때문이었다** — 자세한 근거는
  `results/instructed_freeform_human_review.md` 참고. 과장하지 않기 위해 표본 수(5건, 1회
  샘플링)의 한계도 같은 문서에 명시했다.
- **비용/시간 실측 로깅**: `llm_extractor.py`/`naive_baseline.py`에 토큰(재시도 포함 합계)·
  처리시간 계측을 추가함. 단, 이 계측이 붙기 전에 이미 실행해둔 원래의 7+7건은 재실행하지
  않았다 — 재실행하면 새 샘플링이 나와서 지금까지 문서 전체에서 인용해온 특정 사례(TC-05
  conflict 놓침 등)와 어긋날 위험이 있기 때문이다. 이번에 새로 실행한 것(세 번째 baseline
  5건, conflict_detector.py)은 실측값을 그대로 남긴다: 세 번째 baseline 5건 합계 13,914
  tokens(대략 5회 호출 26.3초), conflict_detector.py는 LLM을 쓰지 않아 토큰 비용 0.

**보류한 것과 이유**
- **D2 오탐(같은 문서를 인용하는 무관한 항목을 중복 확정으로 오판)**: 발견 시점에 이미
  "지금부터는 평가기를 완벽하게 만들려 하지 않는다"고 판단을 내렸고, 이번에도 그 판단을
  유지했다 — 우선순위가 낮아서가 아니라, 매번 발견하는 대로 다 고치면 "언제 끝나는지 모르는
  평가기 다듬기"가 되어 프로젝트의 핵심 메시지(무엇을 실행했고 무엇을 안 했는지 명확히 긋는
  것)를 흐린다고 봤다.
- **paraphrase 매칭**: 동일한 이유로 계속 보류(오탐 위험, 임계값 근거 부족).

이 판단 기준 자체가 이 프로젝트의 핵심 주장과 같다 — 모든 걸 고치는 게 아니라, 측정의 정확성에
영향을 주는 것과 아닌 것을 구분해서 전자만 먼저 고친다.

## 7. 현업가치 지표 1개 (추정 — 실사용자 테스트 아님, 명확히 구분)

실제 설계사 사용자 테스트는 못 했다. 대신 이미 있는 데이터로 **"설계사가 확인해야 할 항목
중 실제로 확인 못 하고 넘어갈 위험이 있는 비율"**을 추정한다.

naive 7건에서 사람이 원문과 직접 대조하기 전까지 놓쳤던(또는 잘못 처리했던) 항목: TC-04(발화
불일치 미지적), TC-05(conflict 미인식), TC-10(환각), TC-15(Critical 항목 완전 누락) — 7건 중
4건에서 설계사가 그대로 믿었다면 실제로 놓쳤을 정보가 있었다(**약 57%**, `results/
naive_baseline_human_review.md` 근거). structured는 같은 7건 중 실패가 2건(TC-05, TC-15의
conflict)이었지만, 그 2건 모두 evaluate.py가 D3 AUTO-FAIL로 **자동으로** 표시해 Human Review
Queue로 넘어갔다 — 즉 "놓칠 위험이 있는 항목의 비율"보다 **"놓쳤을 때 그걸 스스로 신고하는
비율"**이 naive는 0%(사람이 매번 전체를 다시 읽어야 함), structured는 검출된 실패 기준 100%
(2/2 모두 D3로 잡힘)라는 게 더 방어 가능한 지표다.

**추정이지 측정이 아니다**: 실제 설계사가 이 도구를 쓰면서 걸리는 시간, 실제 업무에서의
누락률은 이 15개 가상 시나리오로는 알 수 없다. 이 수치는 "AI에게 맡겼을 때 사람이 무엇을
다시 확인해야 하는지가 명시적으로 좁혀지는가"에 대한 방향성 근거일 뿐이다.

## 8. 비즈니스 연결 — 1개만 고른다

**선택**: 계약 전환 **상담 전 점검 도구**로 연결한다. 설계사가 상담 시작 전에 기존계약·
신규상품 데이터를 넣으면 changes/unknowns/conflicts를 미리 뽑아주고, 상담 중에는 이 목록을
체크리스트처럼 확인하며 설명하게 하는 방식이다.

**이유**: (1) 지금 만든 파이프라인의 입력(customer/conversation/existing_contract/
new_product)과 출력(changes/unknowns/conflicts/evidence)이 이미 이 용도에 그대로 맞는다 —
새 기능을 만들 필요가 없다. (2) 이 프로젝트가 실제로 검증한 것과 정확히 일치한다: "AI가
확인 항목을 놓치지 않고 사람에게 넘기는가." 상담 **중** 실시간 개입이나 **사후** 완전판매
감사는 요구사항(실시간성, 감사 추적)이 다르고 이번 결과물로 그 가치를 주장할 근거가 없다.

**후순위 대안**: "놓치기 쉬운 항목을 AI 롤플레이 교육 사례로 전환"도 가능하지만, 이건 15개
테스트 케이스를 교육 콘텐츠로 재활용하는 것이라 지금 파이프라인의 핵심 가치(검증 구조)와는
결이 다르다 — 언급만 하고 우선순위에서 뺀다.

## 9. 기술적 해자 — 범용 모델이 발전해도 남는 것

**우선순위**: (1) **재현 가능한 평가 데이터(15개 GT + 실패 사례)** → (2) **사람의 수정이
다음 검증에 반영되는 구조** → (3) 항목 연결 기준(스키마) → (4) 현업자가 검토한 오류 사례.

**이유**: 범용 모델이 좋아지면 (3)(4)는 상당 부분 모델이 스스로 잘하게 될 수 있다 — 스키마
설계나 개별 오류 사례는 결국 "지금 모델이 못하는 것"의 기록이라 모델이 좋아지면 가치가
줄어든다. 반면 (1)은 모델이 아무리 좋아져도 "이 모델이 이 도메인에서 실제로 맞는지"를
검증할 방법 자체가 있어야 하고, 그 검증 데이터(GT)는 누군가 직접 설계해야 한다 — 이번
프로젝트에서 가장 시간이 오래 걸린 것도 15개 GT를 설계하고 그 안의 자기모순을 잡는
일이었다. (2)는 이번에 TC-05 Human Review에서 실제로 보여준 것처럼, 사람이 고친 결과가
다음 평가기 규칙(D3, D4, B5)으로 그대로 편입되는 구조다 — 이건 모델을 바꿔도 그대로
남는다. 이 두 가지(재현 가능한 평가 데이터, 사람 수정이 검증에 반영되는 구조)부터 쌓는
게 맞다고 판단한다 — 둘 다 "모델의 능력"이 아니라 "조직이 무엇을 맞다고 확인했는지의
축적"이라 범용 모델 발전과 무관하게 남기 때문이다.
