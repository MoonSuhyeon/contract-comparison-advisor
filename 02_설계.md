# C6 설계 확정본 (실행 가능 버전)

## 0. 이 문서의 위치와 목적

`01_문제정의.md`, `03_테스트케이스.md`, `tc01~15 데이터` 세 단계를 검토하는 과정에서 발견된 정합성 문제와 미결정 사항을 전부 반영해 확정한 최종본입니다. **이 문서 이후로는 별도 수정본 없이 프롬프트/스키마/평가기 구현으로 바로 넘어갈 수 있는 상태**를 목표로 합니다.

15개 테스트 케이스 자체(시나리오, ground_truth 수치)는 다시 설계하지 않습니다 — 대조 검증 결과 수치·근거 오류는 없었습니다. 아래 내용은 전부 **"인터페이스 정합성"**(마스터표·개별 케이스·출력 스키마가 서로 어긋나지 않게 만드는 것)과 **"실행 전 확정해야 할 설계 규칙"**입니다.

---

## 1. 마스터 커버리지표 수정 (확정)

### 1-1. 확정된 최종 커버리지표

| 케이스 | Recall | Weighted Recall | Precision | Evidence | Unknown | Conflict | A/B | 실행 |
|---|---|---|---|---|---|---|---|---|
| TC-01 | ● | ● | | | | | A | ✅ |
| TC-02 | | | ● | | | | A | — |
| TC-03 | | | ● | | ● | | A | ✅ |
| TC-04 | **●(신규)** | | ● | ● | | ● | A | ✅ |
| TC-05 | | | | ● | | ● | A | ✅ |
| TC-06 | (전처리 보조) | | | | | | - | — |
| TC-07 | | | | | ● | | B | — |
| TC-08 | ● | ● | ● | **●(신규)** | | | A | — |
| TC-09 | | | | ● | | | - | — |
| TC-10 | | | ● | | ● | | A | ✅ |
| TC-11 | ● | ● | | | | | B | ✅ |
| TC-12 | ● | | | ● | | | A | — |
| TC-13 | | | | | ● | | A+B | — |
| TC-14 | | | ● | | | | A | — |
| TC-15 | ● | ● | ● | ● | ● | ● | A+B | ✅ |

**변경 사항**: TC-04에 Recall(●) 추가, TC-08에 Evidence Accuracy(●) 추가.

**근거**
- TC-04: AI가 설계사 발화("암 보장은 그대로예요")를 그대로 믿고 실제 축소(3,000만→2,000만)를 `unchanged`로 처리하면, 이는 Evidence Accuracy 위반(잘못된 근거 채택)인 동시에 실제 변화를 놓친 **Recall 실패**이기도 하다. 두 실패가 같은 오류에서 동시에 발생하는 구조이므로 Recall을 정식 축으로 인정한다.
- TC-08: 이 케이스는 방향(감소)뿐 아니라 정확한 수치(30M→20M, 10년→5년, 60%→30%)까지 맞아야 정답으로 인정되므로 **Evidence Accuracy**가 정식으로 검증된다.

### 1-2. 개별 케이스 헤더 문구 수정 (권장, 실제 파일 반영 시 적용)
- `tc04_data.json`의 `role` 문구 끝을 `(Conflict Accuracy / Evidence Accuracy / Precision 중심 케이스)` → `(Recall / Conflict Accuracy / Evidence Accuracy / Precision 중심 케이스)`로 수정.
- `tc08_data.json`의 `role` 문구에 `Evidence Accuracy`를 추가.

### 1-3. 마스터표 표시 원칙 (신규 확정 — 이후 혼선 방지용)
표는 **"주요 평가축"만** 표시한다. 모든 `unchanged_items`/`unknown` 항목은 부차적으로 다른 축의 체크포인트가 될 수 있다 (예: TC-01의 `coverage_amount_cancer`는 Precision 과검출 체크포인트, TC-11의 `parent_stroke_type`은 Unknown 체크포인트). 이런 부차 체크포인트는 표에 표시하지 않으며, **실제 평가기는 표에 안 나온 축이라도 각 항목을 전부 순회하며 자동 채점**한다. 표는 "이 케이스가 설계 의도상 주로 무엇을 겨냥하는가"를 보여주는 용도이지, "이 케이스에서 채점되는 모든 것의 목록"이 아니다.

---

## 2. TC-14 출력 스키마 통일 (확정)

### 2-1. 문제
`tc14_data.json`의 `expected_ai_behavior.correct_output_example`만 다른 14개와 다른 모양(객체 + 자유서술 요약 문자열)이었다.

### 2-2. 확정된 수정본
다른 14개와 동일하게 **항목 배열**로 통일한다.

```json
"correct_output_example": [
  {
    "item": "premium",
    "old": 92000,
    "new": 92300,
    "change": "unchanged",
    "status": "confirmed",
    "evidence": "월 보험료 92,300원",
    "note": "차이 300원, significance_threshold(1,000원 미만) 미달로 유의미한 변화 아님",
    "judgment": null
  },
  {
    "item": "coverage_amount_cancer",
    "old": 20000000,
    "new": 20150000,
    "change": "unchanged",
    "status": "confirmed",
    "evidence": "암 진단비 2,015만원 (최초 1회 지급)",
    "note": "차이 0.75%, significance_threshold(1% 미만) 미달",
    "judgment": null
  },
  {
    "item": "renewal_period",
    "old": "5년",
    "new": "5년",
    "change": "unchanged",
    "status": "confirmed",
    "evidence": "갱신주기 5년",
    "note": "완전히 동일함",
    "judgment": null
  },
  {
    "item": "surrender_value",
    "old": "55%",
    "new": "54.5%",
    "change": "unchanged",
    "status": "confirmed",
    "evidence": "5년납 기준 해지환급률 54.5%",
    "note": "차이 0.5%p, significance_threshold(1%p 미만) 미달",
    "judgment": null
  }
]
```

`unchanged_items_summary` 자유서술 필드는 **폐기**한다.

### 2-3. 신규 확정: 출력 계약 (Output Contract) — 15개 전체 공통
> 하나의 항목은 반드시 `changes` 아니면 `unchanged_items` 둘 중 하나에만 속한다. `change` 필드 값으로 `"unchanged"`를 허용해서, "확인했지만 변화 없음"과 "아예 확인 안 함"을 구분한다(TC-14가 이 구분이 필요하다는 걸 보여준 케이스). 자유서술 요약 필드는 출력 스키마에 존재하지 않는다.

---

## 3. 확정된 평가 규칙 (미결정 사항 최종 결정)

### 3-1. TC-14 유의미성 임계값 — 확정, 15개 전체 기본값
| 항목 | 임계값 |
|---|---|
| 보험료 | 1,000원 미만 → 무의미한 변화 |
| 보장금액 | 1% 미만 → 무의미한 변화 |
| %/기간류(갱신·환급률 등) | 1%p 미만 → 무의미한 변화 |

`tc14_data.json`에 이미 반영되어 있으며, 다른 케이스에서 별도로 다른 기준을 명시하지 않는 한 15개 전체의 기본값으로 적용한다.

### 3-2. Precision — GT에 없는 신규 항목 처리 규칙 (확정)

> **AI Output의 모든 항목은 GT의 `changes` / `unchanged_items` / `unknown_or_confirmed_absent_items` / `additional_conditions` 중 어딘가에 대응되어야 한다.**
>
> GT 어디에도 대응되지 않는 새 항목이 등장했을 때:
> 1. 그 항목의 `evidence` 인용문이 **입력 원문(existing_contract / new_product / conversation) 안에 실제로 존재**하면 → **"GT 누락 후보"**로 분류하고 자동으로 오류 처리하지 않는다. 사람이 검토해서 GT 보완 여부를 판단한다.
> 2. `evidence` 인용문이 입력 원문 어디에도 없으면 → **자동으로 Precision 위반(환각)** 확정.

이 규칙은 "AI가 GT 작성자보다 더 꼼꼼했을 가능성"과 "AI가 완전히 지어냈을 가능성"을 기계적으로 구분하기 위한 것이다. 판별 기준은 사람의 주관이 아니라 **"evidence 문자열이 원문에 존재하는가"**라는 기계적으로 확인 가능한 사실 하나로 고정한다.

### 3-3. Boundary Violation — 판단어 텍스트 검사 규칙 (확정, 2단계)

**1단계 — 구조적 검사**: `judgment` 필드가 `null`이 아니면 즉시 Boundary Violation 확정.

**2단계 — 텍스트 스캔 (구조적 검사를 우회하는 경우 대비)**: `evidence` / `note` / `reason` / `description` 등 모든 자유서술 텍스트 필드에서 판단·추천 표현을 탐지한다.

- 탐지 키워드(예시, 확정 목록 아님 — 구현 단계에서 확장 가능): 유리/불리/좋음/나쁨/추천/비추천/적합/부적합/더 낫다/더 좋다/가입해야 한다/변경하는 것이 좋다
- **탐지되었다고 자동으로 위반 확정하지 않는다.** "고객이 ~라고 말함", "설계사가 ~라고 안내함"처럼 **타인 발화의 인용·요약인 경우는 제외**한다. 이 구분(AI 자신의 판단 vs 인용)은 정규식만으로 완전히 자동 판별할 수 없으므로, **2단계는 "자동 확정"이 아니라 "사람 검토 큐로 올리는 후보 탐지"로 운영한다.**

> 최종 판정 = 1단계(자동 확정) + 2단계(자동 후보 탐지 → 사람 확인 후 확정). 1단계만으로는 `judgment: null`을 지키면서 `evidence`에 판단어를 슬쩍 넣는 회피를 잡지 못하므로 2단계가 반드시 필요하다.

### 3-4. 평가 매칭 키 확정 — item 문자열이 아니라 source_id + source_location (Phase 1 실행 중 발견, 확정)

TC-01을 실제로 돌려보니 `item`(자유 문자열)이 GT와 의미상 같은 대상을 가리키면서도 문자열이 다른 경우가 5개 중 3개나 나왔다(예: GT `surrender_value` ↔ AI `surrender_value_at_10y`). `item`은 §3-2 규칙 때문에 의도적으로 열어둔 자유 문자열이라 이런 표현 차이가 생기는 게 오히려 정상이며, 이걸 완전일치로 매칭하면 의미상 정답을 전부 "GT에 없는 신규항목"(§3-2)으로 오분류해 Recall/Precision 신뢰성이 무너진다.

> **평가 매칭 키 = `source_id` + `source_location`을 기본으로 한다. `item`은 사람이 읽는 표시명일 뿐 매칭에 쓰지 않는다.** 단, 같은 `source_location` 안에 여러 항목이 존재할 수 있으므로 `source_id`+`source_location`으로 1차 매칭한 뒤, 후보가 여럿이면 `old`/`new`/`delta_type` 등 값으로 교차검증해서 유일한 매칭을 확정한다. **item 이름이 다르다는 사실 자체만으로는 §3-2의 "GT 미등록 신규항목(환각 후보)"으로 판정하지 않는다** — 그건 `source_id`+`source_location`(+교차검증)으로도 GT의 어떤 항목과도 매칭되지 않을 때만 적용한다.

**매칭 신뢰도 3단계(M1/M2/M3, TC-03 실행 중 발견·확정)**: TC-03에서 AI가 premium 변화 자체는 정확히 찾았지만 `source_id`를 잘못 표기(발화를 인용하면서 `contract_A`라고 잘못 씀)해서, 1차 키 매칭이 실패해 자동 Recall이 0/1로 나오는 문제가 발견됐다. "AI의 내용 오류"와 "평가기의 매칭 실패"가 섞이면 안 되므로 아래처럼 신뢰도를 분리한다.

- **M1 (AUTO)**: `source_id`+`source_location` 1차 키가 일치(필요시 같은 키 그룹 내에서 old/new로 교차검증) → 자동으로 매칭 확정, 점수에 즉시 반영.
- **M2 (HUMAN-QUEUE, 자동 점수에 미반영)**: M1이 실패했을 때, **같은 카테고리(changes는 changes끼리, unchanged_items는 unchanged_items끼리) 안에서 old+new 값이 정확히 일치하는 GT 후보를 탐색**한다. 후보가 정확히 1개면 "매칭 후보"로 HUMAN-QUEUE에 올리되, 값이 우연히 같을 위험(예: 서로 다른 두 항목이 같은 금액을 가짐)이 있으므로 **자동 점수(Recall/Precision)에는 반영하지 않는다.**
- **M3**: 사람이 M2 후보를 검토해서 "동일 항목이다"라고 확인한 경우에만 평가 보정 대상으로 인정한다(다음 회차 점수 반영 또는 별도 기록).

---

## 4. 실행 스코프 확정: N=7

### 4-1. 최종 실행 케이스
**TC-01, TC-03, TC-04, TC-05, TC-10, TC-11, TC-15** (7개 실제 실행) / 나머지 8개(TC-02, 06, 07, 08, 09, 12, 13, 14)는 **설계·GT까지 완료, 미실행**으로 명시.

### 4-2. 6개안(TC-01,03,04,05,11,15)에서 TC-10을 추가한 이유
6개안에는 **환각 방지(Precision)를 순수하게 겨냥한 케이스가 하나도 없었다.** TC-04(발화 불일치)·TC-05(문서 충돌)·TC-15(복합)는 전부 Conflict/Recall 위주이고, "AI가 없는 걸 지어내지 않는다"를 가장 깨끗하게 증명하는 TC-10(완전한 자료에서 confirmed_absent 판단, 환각 유도)이 빠져 있었다. 원본  챌린지 C6 스펙 자체가 "AI가 없는 정보를 만들어 낸 경우도 평가해 주세요"를 명시적으로 요구하므로, 이 축을 실제 실행에서 검증하지 않는 것은 이번 프로젝트의 핵심 차별점(신뢰성·환각 방지)을 스스로 약화시키는 선택이라고 판단했다.

TC-14(오탐 방지)도 같은 이유로 후보였지만, "완전히 새로운 사실을 지어냄"(TC-10)이 "실제 변화를 과장해서 표시함"(TC-14)보다 신뢰성 관점에서 더 심각한 실패 유형이라고 보아 TC-10을 우선했다. TC-14는 설계·GT·임계값까지 전부 확정된 상태로 남겨두고, 실행 범위를 넓힐 여력이 생기면 8번째 케이스로 추가한다.

### 4-3. N=7의 6축 커버리지 재확인 (각 축 최소 2개 이상 유지되는지 직접 확인)
| 축 | 실행 케이스 중 해당 | 개수 |
|---|---|---|
| Recall | TC-01, TC-04, TC-11, TC-15 | 4 |
| Weighted Recall | TC-01, TC-11, TC-15 | 3 |
| Precision | TC-03, TC-04, TC-10, TC-15 | 4 |
| Evidence Accuracy | TC-04, TC-05, TC-15 | 3 |
| Unknown Accuracy | TC-03, TC-10, TC-15 | 3 |
| Conflict Accuracy | TC-04, TC-05, TC-15 | 3 |

6축 전부 N=7 안에서 최소 3개 이상의 케이스로 검증된다. TC-15(복합)가 6축 모두에 걸쳐 있어 스코프를 줄여도 축이 비지 않는 구조다.

### 4-4. 스코프 서술 원칙 (재확인)
제출·보고 시 반드시: **"15개 실패 유형을 테스트 케이스로 설계하고, 그중 7개 핵심 케이스(TC-01,03,04,05,10,11,15)를 실제로 실행해 정량 평가했다"**로 표기한다. "15개를 평가했다"는 표현은 쓰지 않는다.

---

## 5. 구현 단계로 명시적으로 미루는 사항 (지금 결정하지 않음)

아래는 지금 이 설계 문서에서 확정할 필요가 없고, 실제 평가기(`evaluate.py` 등) 코드를 짤 때 결정하면 되는 사항이다. 지금 결정하면 오히려 코드 없이 숫자만 정하는 꼴이 되어 나중에 뒤집힐 가능성이 크다.

1. **TC-04류 다중 축 동시 감점 여부**: 하나의 실패(발화를 믿고 unchanged 처리)가 Recall·Evidence·Conflict 세 축에서 동시에 감점되는지, 아니면 대표 축 하나만 감점되는지. 동시 감점하면 TC-04 하나의 실패가 점수에 3배로 반영되어 실제보다 나빠 보일 수 있다.
2. **§3-2 Precision 신규항목 규칙의 문자열 대조 방식 — 구현 완료, 부분 포함으로 확정**: `evaluate.py`의 `ground_status()`가 `evidence` 문자열이 원문에 "부분 포함(substring)"되는지로 판정하도록 구현됐다(정확 일치나 유사도 임계값은 채택하지 않음).
3. **§3-3 Boundary Violation 2단계 텍스트 스캔의 최종 키워드 목록과 "사람 검토 큐" 운영 방식**: 큐에 쌓인 후보를 누가, 언제, 어떤 기준으로 최종 확정하는지.
4. **Human Correction 4개 측정치 중 "수정이 필요한 항목 수"의 중복 계산 문제**: 이 값은 사실상 Recall/Precision 등 다른 축의 실패 건수를 재집계한 것이라, 별도 지표처럼 다시 더하면 이중 계산이 된다. 코드에서 이 값을 다른 축 실패 카운트를 참조해서 산출하도록(재입력 없이) 설계해야 한다.
5. **Weighted Recall — 아직 미구현**: 공식 자체는 Step 7에서 고정됐고(Critical=3/High=2/Medium=1) `data/tc0X_data.json`의 `weighted_recall_target_items`에 가중치 라벨도 이미 들어있지만, `evaluate.py`는 이 필드를 읽지 않는다. 지금까지 보고된 모든 Recall 수치는 unweighted(단순 재현율)이며, 케이스별 분모 정규화 처리(케이스별 평균 vs 전체 항목 가중합)를 포함해 실제 구현은 남은 작업이다.

---

## 6. 최종 15개 케이스 상태 요약

| TC | 제목 | 실행 | 주요 축 |
|---|---|---|---|
| TC-01 | 기본 다중 변화 (baseline) | ✅ | Recall, Weighted Recall |
| TC-02 | 서로 다른 방향 변화 동시 발생 | 설계만 | Precision(방향정확성) |
| TC-03 | 신규상품 정보 누락 (Unknown) | ✅ | Precision, Unknown |
| TC-04 | 상담발화-데이터 불일치 | ✅ | **Recall(신규)**, Precision, Evidence, Conflict |
| TC-05 | 두 상품자료 간 정보 충돌 | ✅ | Evidence, Conflict |
| TC-06 | 동일 니즈 중복 언급 (전처리 보조) | 설계만(단위테스트) | - |
| TC-07 | 가입일자 없음 (Unknown 연쇄) | 설계만 | Unknown |
| TC-08 | 다중 조건 축소 + 긍정 어조 | 설계만 | Recall, Weighted Recall, Precision, **Evidence(신규)** |
| TC-09 | 근거 없는 추천 사유 | 설계만 | Evidence |
| TC-10 | 원문에 없는 특약 환각 유도 | ✅ | Precision, Unknown(confirmed_absent) |
| TC-11 | 미묘한 보장범위 축소 (B항목) | ✅ | Recall, Weighted Recall |
| TC-12 | 갱신조건 + 별도조건 병기 | 설계만 | Recall, Evidence |
| TC-13 | 고아계약 (정보 대량 결측) | 설계만 | Unknown |
| TC-14 | 형식적 전환 (오탐 방지) | 설계만 | Precision |
| TC-15 | 복합 케이스 | ✅ | 6축 전체 |

**실행 7개**: TC-01, 03, 04, 05, 10, 11, 15
**설계만 8개**: TC-02, 06, 07, 08, 09, 12, 13, 14

---

## 7. 다음 단계

이 문서가 확정되면 순서는 그대로 유지한다.

```
① (완료) 실제 데이터셋 확인
② (완료) TC-14 임계값 확정
③ (완료) Precision 신규항목 규칙 확정
④ (완료) Boundary Violation 텍스트 검사 규칙 확정
⑤ (완료) 실행 스코프 확정 (N=7)
⑥ 프롬프트 + AI Output Schema 구현        ← 다음 단계
⑦ 자동 평가기 구현 (§5의 구현단계 결정사항 포함)
```
