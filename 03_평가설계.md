# 평가 설계

AI가 무엇을 해야 하는지는 `01_문제정의.md`에서 정했습니다.

AI는 기존 계약과 신규 계약의 차이를 추출하고 비교합니다. 그중 고객에게 불리한 변화는 표시하고, 확인할 수 없는 정보는 따로 표시하며, 판단 근거가 되는 계약 내용을 함께 연결합니다.

그다음 단계에서 더 어려웠던 것은 AI가 낸 결과를 컴퓨터가 어떻게 자동으로 채점할 것인가였습니다.

AI가 GT와 같은 대상을 다른 이름으로 표기할 수도 있고, 보험료 1,000원 차이처럼 사소한 변화까지 전부 오류로 볼 수도 있으며, 스스로 판단(유리/불리 등)을 슬쩍 끼워넣을 수도 있습니다.

**따라서 `evaluate.py`를 구현하기 전에 무엇을 정답으로 인정하고 어떤 기준으로 채점할지를 먼저 정해야 했습니다.** 여기에 더해, 15개 케이스를 전부 실행할 시간이 없어 그중 일부만 고르는 기준도 필요했습니다.

그래서 먼저 평가 규칙을 정하고, 그 기준에 맞게 테스트 케이스를 점검했습니다. 이후 실제 실행 범위를 정하고 평가기를 구현했습니다. 이 과정에서 결정한 내용과 그 근거를 이 문서에 정리했습니다.

**예시로 먼저 보기**

GT(정답)의 changes 항목 하나(TC-05):
```json
{ "item": "surrender_value", "source_id": "product_B",
  "source_location": "product_terms_detail", "old": "45%", "new": "35%" }
```

같은 사실을 AI가 낸 항목:
```json
{ "item": "surrender_value_at_10y", "source_id": "product_B",
  "source_location": "product_terms_detail", "old": "45%", "new": "35%" }
```

`item` 이름은 다르지만(`surrender_value` vs `surrender_value_at_10y`) `source_id`+`source_location`이 똑같아서 이 둘은 같은 대상으로 자동 매칭됩니다(§1-4의 M1). 뒤에 나오는 changes/unchanged_items, source_id+source_location, M1/M2/M3는 전부 이 JSON 항목 하나를 어떻게 다루느냐에 대한 이야기입니다.

## 1. 평가 규칙

### 1-1. 유의미성 임계값

보험료는 1,000원 미만, 보장금액은 1% 미만, 비율/기간류(갱신·환급률 등)는 1%p 미만 차이는 "무의미한 변화"로 봅니다. 15개 전체의 기본값입니다.

### 1-2. GT에 없는 새 항목 처리

AI가 GT 어디에도 없는 항목을 냈을 때, 그 항목의 근거 문장이 원문에 실제로 있으면 "GT 누락 후보"로 분류해 사람이 검토하고, 원문 어디에도 없으면 자동으로 환각(Precision 위반)으로 확정합니다.

판별 기준은 사람의 주관이 아니라 "근거 문자열이 원문에 존재하는가" 하나로 고정합니다.

### 1-3. 판단어 검사 (Boundary Violation)

`judgment` 필드가 `null`이 아니면 즉시 위반 확정입니다. 그것만으로는 `evidence`/`note` 같은 다른 텍스트 필드에 판단어를 슬쩍 끼워넣는 걸 못 잡기 때문에, 유리/불리/추천 같은 판단 표현이 등장하는지 텍스트를 한 번 더 스캔합니다.

다만 "고객이 ~라고 말함"처럼 남의 발화를 인용한 경우까지 자동으로 위반 처리하면 안 되므로, 이 2단계는 자동 확정이 아니라 사람이 확인할 후보를 올리는 용도로만 씁니다.

### 1-4. 매칭 키

평가는 항목 이름(`item`)이 아니라 `source_id`+`source_location`으로 맞춥니다. TC-01(기본 다중 변화)을 실제로 돌려보니 같은 대상을 가리키면서도 이름이 다른 경우가 5개 중 3개나 나와서(예: GT `surrender_value` ↔ AI `surrender_value_at_10y`), 이름으로만 매칭하면 이런 항목들을 "GT에 없는 새 항목"으로 잘못 판정하게 됩니다.

**매칭 신뢰도는 3단계로 나눴습니다.** M1과 M2가 실제로 어떻게 다른지 TC-03(신규상품 정보 누락)의 실제 사례로 보면:

GT: `{ "item": "premium", "source_id": "product_B", "source_location": "product_brochure_p1", "old": 95000, "new": 88000 }`
AI: `{ "item": "premium", "source_id": "contract_A", "source_location": "msg_03_06", "old": 95000, "new": 88000 }`

`item`은 똑같이 "premium"이지만 `source_id`+`source_location`이 서로 달라(AI가 발화를 인용하면서 출처를 기존계약으로 잘못 표기) M1 매칭에 실패합니다. 하지만 같은 카테고리(`changes`) 안에서 `old`+`new` 값(95000→88000)이 정확히 일치하는 GT 후보가 1개 있어서, 이 항목은 M2(사람 확인 큐)로 올라갑니다 — 자동 점수에는 아직 반영되지 않고, 사람이 "출처 표기만 틀렸을 뿐 같은 항목"임을 확인해야 M3로 넘어갑니다.

| 단계 | 조건 | 처리 |
|---|---|---|
| M1 | source_id+source_location이 일치 | 자동 확정, 점수 즉시 반영 |
| M2 | M1 실패, 같은 카테고리 안에서 old+new 값이 일치하는 후보 1개 발견 | 사람 확인 큐로만(자동 점수 미반영 — 값이 우연히 같을 위험) |
| M3 | 사람이 M2 후보를 확인 | 그때 평가에 반영 |

이 3단계는 TC-03에서 AI가 내용은 맞혔는데 출처(source_id)를 잘못 표기해 자동 Recall이 0으로 나온 걸 발견하면서 확정했습니다. "AI의 내용 오류"와 "평가기의 매칭 실패"를 섞지 않기 위해서입니다.

## 2. 테스트 케이스

아래 표의 TC 번호는 이 문서 전체에서 계속 사용합니다. 뒤에서 TC-04, TC-01 같은 번호가 나올 때 이 표를 기준으로 확인하면 됩니다.

| TC | 제목 | 실행 | 주요 평가 축 |
|---|---|---|---|
| TC-01 | 기본 다중 변화 | ✅ | Recall, Weighted Recall |
| TC-02 | 서로 다른 방향 변화 동시 발생 | 설계만 | Precision(방향정확성) |
| TC-03 | 신규상품 정보 누락 | ✅ | Precision, Unknown |
| TC-04 | 상담발화-데이터 불일치 | ✅ | Recall, Precision, Evidence, Conflict |
| TC-05 | 두 상품자료 간 정보 충돌 | ✅ | Evidence, Conflict |
| TC-06 | 동일 니즈 중복 언급 | 설계만 | - |
| TC-07 | 가입일자 없음(Unknown 연쇄) | 설계만 | Unknown |
| TC-08 | 다중 조건 축소 + 긍정 어조 | 설계만 | Recall, Weighted Recall, Precision, Evidence |
| TC-09 | 근거 없는 추천 사유 | 설계만 | Evidence |
| TC-10 | 원문에 없는 특약 환각 유도 | ✅ | Precision, Unknown |
| TC-11 | 미묘한 보장범위 축소 | ✅ | Recall, Weighted Recall |
| TC-12 | 갱신조건 + 별도조건 병기 | 설계만 | Recall, Evidence |
| TC-13 | 고아계약(정보 대량 결측) | 설계만 | Unknown |
| TC-14 | 형식적 전환(오탐 방지) | 설계만 | Precision |
| TC-15 | 복합 케이스 | ✅ | 6축 전체 |

실행 7개: TC-01, 03, 04, 05, 10, 11, 15 / 설계만 8개: TC-02, 06, 07, 08, 09, 12, 13, 14

※ 표의 주요 평가 축은 각 케이스의 설계상 검증 대상이며, 실제 실행·산출 여부와는 다를 수 있습니다.

§1의 규칙을 실제 케이스에 적용하는 과정에서 TC-04, TC-08, TC-14의 설계를 일부 수정했습니다. 위 표와 §3의 실행 범위는 이 수정이 반영된 최종 상태이며, 수정 내용과 근거는 §4에 정리했습니다.

## 3. 실행 범위

실제로 실행하는 건 7개(TC-01, 03, 04, 05, 10, 11, 15)이고, 나머지 8개(TC-02, 06, 07, 08, 09, 12, 13, 14)는 설계·GT까지 완료한 상태로 남겨둡니다.

처음 검토했던 6개안(TC-01,03,04,05,11,15)에는 "AI가 없는 걸 지어내지 않는가"만 순수하게 겨냥하는 케이스가 없어서, TC-10(완전한 자료에서 confirmed_absent 판단, 환각 유도)을 추가했습니다. 환각 방지는 이 프로젝트에서 확인하려는 주요 실패 유형 중 하나이므로 실제 실행에서 포함했습니다. TC-14(오탐 방지)도 후보였지만, 이번 실행에서는 원문에 없는 정보를 생성하는 환각 여부를 우선적으로 확인하기 위해 TC-10을 선택했습니다.

**이 7개가 6개 평가축을 얼마나 커버하는지도 확인했습니다.**

| 축 | 실행 케이스 | 개수 |
|---|---|---|
| Recall | TC-01, 04, 11, 15 | 4 |
| Weighted Recall | TC-01, 11, 15 | 3 |
| Precision | TC-03, 04, 10, 15 | 4 |
| Evidence Accuracy | TC-04, 05, 15 | 3 |
| Unknown Accuracy | TC-03, 10, 15 | 3 |
| Conflict Accuracy | TC-04, 05, 15 | 3 |

6축 전부 최소 3개 이상의 케이스로 검증됩니다. TC-15(복합 케이스) 하나가 6축 전체에 걸쳐 있어서, 스코프를 줄여도 축이 비지 않습니다.

제출·보고할 땐 "15개 실패 유형을 설계하고 그중 7개를 실제로 실행해 평가했다"로 표기하고, "15개를 평가했다"는 표현은 쓰지 않습니다.

## 4. 케이스 정합성 수정

§2에서 예고한 대로, 위 규칙을 실제 케이스에 적용해보면서 세 가지 정합성 문제를 발견해 고쳤습니다.

- **TC-04**: 설계사 발화("암 보장은 그대로예요")를 그대로 믿고 실제 축소(3,000만→2,000만원)를 놓치면, 실제 계약 내용과 다른 발화를 근거로 삼으면 Evidence Accuracy 위반이고, 실제 보장 축소 자체를 놓치면 Recall 실패이기도 합니다. 원래 커버리지표에 Recall이 빠져 있어서 추가했습니다.
- **TC-08**: 방향(감소)뿐 아니라 정확한 수치(3천만→2천만원, 10년→5년, 60%→30%)까지 맞아야 정답으로 인정되는 케이스라, Evidence Accuracy를 추가했습니다.
- **TC-14**: 다른 14개 케이스와 달리 정답 예시가 자유서술 요약 문자열로 돼 있었습니다. 다른 케이스와 똑같이 항목 배열 형태로 통일했고, 자유서술 요약 필드는 출력 스키마에서 폐기했습니다. 하나의 항목은 반드시 `changes` 아니면 `unchanged_items` 둘 중 하나에만 속합니다(`changes`는 Recall의 분모, `unchanged_items`는 Precision 과검출 체크포인트로 씁니다).

커버리지표(§2)는 각 케이스가 "설계 의도상 주로 무엇을 겨냥하는가"만 보여줍니다. 표에 없는 축이라도 평가기는 모든 항목을 순회하며 자동 채점합니다.

## 5. 구현 과정에서 남은 결정 사항

아래는 이 설계 단계에서는 정하지 않고, 실제 평가기를 구현하면서 결정하기로 미뤄둔 것들입니다. 전부 아직 정하지 않은 것만 담았습니다(이미 정한 것은 §1에 있습니다). 코드 없이 숫자만 먼저 정하면 나중에 뒤집힐 가능성이 크기 때문입니다.

- **다중 축 동시 감점 여부**: 발화를 믿고 unchanged로 처리한 하나의 실패가 Recall·Evidence·Conflict 세 축에서 동시에 감점되는지, 대표 축 하나만 감점되는지.
- **판단어 텍스트 스캔의 키워드 목록과 사람 검토 큐 운영 방식**: 큐에 쌓인 후보를 누가, 언제, 어떤 기준으로 최종 확정하는지.
- **Human Correction 지표의 중복 계산 문제**: "수정이 필요한 항목 수"는 사실 다른 축의 실패 건수를 재집계한 것이라, 별도로 더하면 이중 계산이 됩니다.
- **Weighted Recall의 분모 정규화 방식**: 공식(Critical=3/High=2/Medium=1)과 GT의 가중치 라벨은 준비돼 있지만, 케이스별 분모를 어떻게 종합할지(케이스별 평균 vs 전체 항목 가중합)는 아직 정하지 않았고, `evaluate.py`도 이 값을 읽지 않아 지금까지 나온 모든 Recall 수치는 unweighted입니다.
