# 04. 검증 결과

> 이 문서는 구현 이후 실제로 무엇을 검증했고, 그 과정에서 무엇을 발견·수정했는지를 한눈에
> 보여주는 요약 보고서다. 각 항목의 상세 원본 기록은 문서 끝의 "상세 검증 기록"에 링크해뒀다.

## 1. 검증 개요

**무엇을 검증했는가**: 두 가지를 동시에 검증했다 — (1) 구조화 파이프라인(structured)의 출력이
Ground Truth와 얼마나 일치하는지, (2) 그 채점을 수행하는 평가기(`evaluate.py`) 자체가 정확히
동작하는지. 후자를 같이 검증한 이유는, 평가기 코드에 버그가 있으면 (1)의 결과 자체를 믿을 수
없기 때문이다.

**실행 범위**: 설계한 15개 테스트 케이스 중 7개(TC-01, 03, 04, 05, 10, 11, 15)를 구조화
파이프라인 + naive baseline 양쪽으로 실행했다. 그중 5개(TC-03, 05, 10, 11, 15)는 세 번째
baseline(지침 동일 + 자유서술)까지 추가로 실행했다.

**평가 기준**: `02_설계.md`/`03_테스트케이스.md`에서 확정한 6축 — Recall(Weighted Recall
포함), Precision, Evidence Accuracy, Unknown Accuracy, Conflict Accuracy, Human Correction.

---

## 2. 평가기 자체 검증

실제로 N=7을 순서대로 돌리면서, 평가기 코드 자체의 결함 9건을 발견·수정했다.

| # | 문제 | 발견 계기 | 조치 |
|---|---|---|---|
| 1 | 인코딩 크래시 | Windows 콘솔 실행 | `stdout` UTF-8 재설정 |
| 2 | B1 오분류(환각 vs 출처오귀속 미구분) | TC-03 | `ground_status()`를 `grounded`/`wrong_source`/`not_found` 3분류로 분리 |
| 3 | M1 단독 매칭이 너무 엄격함(source_id 오귀속 시 Recall 0) | TC-03 | M2(값 기반 폴백, HUMAN-QUEUE 전용) 도입 |
| 4 | D1/D2 conflict 매칭 키 버그(`match_key()`를 conflict 객체에 오적용) | TC-04 | `sources[]` 내부 키 집합 비교로 교체 |
| 5 | 중복매칭으로 Recall 150% | TC-11 | `consumed_gt_ids`로 GT 항목당 1회만 인정 |
| 6 | D3(Conflict Recall) 규칙 부재 — conflict를 놓치고 임의 확정해도 무검출 | TC-05 | Conflict Recall(D3) 규칙 신설 |
| 7 | D4/A11 규칙 부재 — unknown 누락·자기모순 무검출 | TC-15 | Unknown Recall(D4) + 자기모순 검사(A11) 신설 |
| 8 | B5(값-근거 일치) 규칙 부재 — 근거 문장은 진짜인데 선언값이 달라도 무검출 | 회귀 테스트(999,999원 값 위조 주입) | 값-근거 정합성(B5) 신설 |
| 9 | 재현성 버그 — 원본 평가결과 파일에 교정본 평가가 남아있었음 | B5 작업 중 | `ai_output_path` 인자로 임의 파일 지정 평가, 출력 파일명 폴더명 접두 분리 |

**수정 후 확인**: N=7 전체를 재실행해 새 규칙(B5 등)이 기존 통과 케이스에 새 오탐을 만들지
않는지 확인했고, fixture 3종(정상/실제conflict/값위조)으로 수정 전·후를 대조했다. 상세:
`results/evaluator_validation_log.md`.

---

## 3. AI 결과 검증

**Structured**: 7건 실행, 자동 채점(6절의 표 참고). Evidence 원문 존재율은 7건 전부 100%
(환각 0건).

**Naive Baseline**: 스키마 없이 자유 텍스트로 7건 실행, 사람이 원문·GT와 직접 대조해 6개
기준(변경사항 발견/방향·값 정확/근거 명시/추론 안 함/충돌 보존/유불리 미판단)으로 채점.
근거 명시 0/7 완전 충족, 출처 충돌 보존 0/3 전부 실패, 유불리 미판단 1/7만 준수 — structured와
가장 크게 갈린 지점이다. 상세: `results/naive_baseline_human_review.md`.

**Instructed Freeform Baseline**: structured와 동일한 지침(근거·충돌·판단경계)을 주되 스키마
강제 없이 자유 텍스트로 받은 세 번째 축. held-out 5건(TC-03,05,10,11,15)에 실행한 결과, naive
실패의 상당 부분이 스키마가 아니라 지침 부재 때문이었음을 확인했다(TC-05 conflict 명시적 분류
성공, TC-10 환각 안 함, TC-15 Critical 항목 포착). 5건·1회 샘플링이라 일반화하지 않는다.
상세: `results/instructed_freeform_human_review.md`.

**Human Review**: TC-05에서 AI가 conflict를 놓치고 35%로 임의 확정한 걸 evaluate.py가 D3
AUTO-FAIL로 검출 → 사람이 원문 2건을 직접 대조해 conflicts로 재분류 → 재검증에서 D3는
사라졌지만 예상 못 한 D2 오탐이 새로 발견됨(7절 참고). 상세: `results/corrected/TC-05.json`.

---

## 4. Conflict 검증

**계약 데이터와 상담 내용의 충돌(source_discrepancy)**: 설계사 발화와 실제 계약 데이터가
어긋나는 경우다. TC-04("암 보장은 그대로예요" vs 실제 3,000만→2,000만 축소)에서는 D1 규칙이
정상 검출했고, TC-15("상해 보장 그대로예요" vs 실제 1,000만→500만 축소)에서는 D3가 AI의
미검출을 정확히 잡아냈다 — AI가 이 유형을 매번 놓치는 건 아니지만 재현 가능한 실패 패턴임을
보여준다.

**GT 없는 Conflict Detector 검증 결과**: `src/conflict_detector.py`는 GT도 LLM도 쓰지 않고
원문 텍스트(%/원 수치 + 한글 2-gram 유사도)만으로 문서 간 충돌을 탐지한다. TC-05(40% vs 35%)를
정확히 탐지했고, 나머지 6건은 오탐 0건이었다(TC-04/15의 conflict는 발화-데이터 불일치 유형이라
이 탐지기의 설계상 대상이 아님 — 의도된 범위 제한). 상세: `results/conflict_detector_log.md`.

---

## 5. 반복 검증 과정

**TC-01 v1 → v2**: 첫 실행(v1)에서 4개 실질 변화 중 3개가 `changes`/`unchanged_items` 배열에
정반대로 들어가는 오류를 발견했다. `old`/`new`/방향 값 자체는 100% 정확해서, 비교 능력이
아니라 배열 라우팅만 실패한 것으로 확인됐다. 원인을 "필드명 `change`와 배열명 `changes`의
문자열 충돌"로 가설을 세우고, `delta_type`으로 개명 + 배치 규칙 명시 + 5단계 자체검사 절차를
추가해 재실행(v2)한 결과 3/4가 개선됐다. 잔여 1건은 원인을 가설로만 남기고 v3를 만들지 않기로
결정했다 — 프롬프트를 오류가 없어질 때까지 계속 고치는 것 자체가 이 프로젝트의 메시지(오류를
어떻게 감지·통제하는가)에서 벗어난다고 판단했기 때문이다. 이 잔여 오류는 이후 실제로 평가기의
B4 규칙이 정확히 검출했다. 상세: `results/tc01_iteration_log.md`.

---

## 6. 최종 결과

structured 파이프라인 7건 실측(`evaluate.py` 자동 채점):

| TC | Recall | Precision | Evidence | AUTO-FAIL | HUMAN-QUEUE |
|---|---|---|---|---|---|
| TC-01 | 3/4 (0.75) | 1.0 | 1.0 | 1 (B4) | 0 |
| TC-03 | 0/1 (0.0)* | 1.0 | 1.0 | 0 | 4 |
| TC-04 | 2/2 (1.0) | 1.0 | 1.0 | 2 (A8, A11) | 1 |
| TC-05 | 1/1 (1.0) | 1.0 | 1.0 | 1 (D3) | 1 |
| TC-10 | 1/1 (1.0) | 1.0 | 1.0 | 0 | 0 |
| TC-11 | 2/2 (1.0) | 1.0 | 1.0 | 1 (D4) | 1 |
| TC-15 | 3/4 (0.75) | 1.0 | 1.0 | 4 (A11,D3,D4×2) | 2 |

\* TC-03의 Recall 0/1은 AI가 값을 틀려서가 아니라 `source_id`를 오귀재해 M1 매칭이 실패한
경우다(M2로 값 일치는 확인됨, HUMAN-QUEUE로만 처리 — §2의 M1/M2/M3 설계에 따른 의도된 결과).

**Naive vs Structured vs Instructed-freeform** 요약 비교는 3절 참고, 전체 수치는
`results/metadata/*.json`과 두 human-review 문서 원본 참고.

---

## 7. 확인된 한계

- **15개 중 7개만 실행**: 나머지 8개(TC-02,06,07,08,09,12,13,14)는 설계·GT까지 완성됐으나
  미실행.
- **Weighted Recall 미구현**: 공식과 GT 가중치 데이터(`weighted_recall_target_items`)는
  준비돼 있으나 `evaluate.py`가 아직 읽지 않는다. 위 표의 모든 Recall 수치는 unweighted다.
- **paraphrase 매칭 미구현**: 표현은 다르지만 의미가 같은 경우(TC-15의
  `coverage_scope_cancer`)는 문자열 기반 매칭에서 미매칭으로 판정된다. 오탐 위험과 임계값
  근거 부족으로 의도적으로 보류.
- **D2 false positive**: 같은 문서를 인용하는 무관한 항목을 중복 확정으로 오판하는 경우가
  있음(TC-05 Human Review 검증 중 발견). 발견 즉시 "평가기를 완벽하게 다듬지 않는다"고
  판단해 의도적으로 미해결.
- **baseline 표본 제한**: 세 번째 baseline(지침+자유서술) 비교는 held-out 5건·1회 샘플링
  결과라 일반화하지 않는다.
- **실제 사용자 검증 미실시**: 설계사가 실제로 이 도구를 썼을 때의 시간 단축·누락률은
  가상 시나리오 기반 추정치일 뿐, 실사용자 테스트는 하지 않았다.

---

## 8. 결론

**확인한 것**: 스키마·검증 규칙을 강제해도 AI는 여전히 실수(conflict 임의 확정, 배열 오분류
등)할 수 있다는 것, 그리고 그 실수를 사람이 매번 원문을 대조하지 않고도 평가기가 자동으로
검출해 Human Review로 넘길 수 있다는 것. Naive와 Structured의 단순 발견 능력 차이는 크지
않았지만, 근거 명시·충돌 보존·판단 경계 준수에서는 뚜렷하게 갈렸고, 그 차이의 상당 부분이
스키마가 아니라 명시적 지침 자체에서 온다는 것도 세 번째 baseline으로 확인했다.

**아직 확인하지 못한 것**: 실제 /GA 상담 데이터에서 이런 오류 패턴들의 실제 발생 빈도,
Weighted Recall/paraphrase 매칭까지 구현했을 때의 최종 정밀도, 실제 설계사가 이 도구를 썼을
때의 업무 효과.

---

### 상세 검증 기록

- 평가기 자체 검증: `results/evaluator_validation_log.md`
- Naive Baseline Human Review: `results/naive_baseline_human_review.md`
- Instructed Freeform Baseline Human Review: `results/instructed_freeform_human_review.md`
- GT 없는 Conflict Detector 검증: `results/conflict_detector_log.md`
- TC-01 반복 검증(v1→v2): `results/tc01_iteration_log.md`
