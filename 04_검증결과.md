# 검증결과

이 문서는 구현 이후 실제로 무엇을 검증했고, 그 과정에서 무엇을 발견·수정했는지를 한눈에 보여주는 요약 보고서입니다. 각 항목의 상세 원본 기록은 문서 끝의 "상세 검증 기록"에 링크해뒀습니다.

## 1. 한눈에 보기

**이 검증에서 확인한 세 가지**

1. **AI는 스키마·규칙을 강제해도 여전히 실수합니다.**

conflict를 놓치고 값을 임의로 확정하거나(TC-05), 배열을 잘못 분류하는(TC-01) 식입니다. 다만 그 실수를 사람이 매번 원문을 대조하지 않고도, 평가기가 자동으로 걸러내 Human Review로 넘길 수 있다는 것도 함께 확인했습니다.

2. **"스키마를 강제하는 것"과 "지침을 명확히 주는 것"은 서로 다른 효과를 낸다는 걸 분리해서 확인했습니다.**

naive(스키마도 지침도 없음)와 structured(둘 다 있음)의 차이 중 상당 부분은, 스키마가 아니라 근거·충돌·판단경계를 명시한 지침 자체에서 왔습니다. 이 사실은 세 번째 baseline(지침은 동일하게 주되 스키마만 뺀 자유서술)을 따로 실행해서 확인했습니다.

3. **채점 도구 자체도 믿을 수 없어서, 그것부터 검증했습니다.**

평가기(`evaluate.py`)를 실제로 7건 돌리는 과정에서 평가기 코드 자체의 버그 9건을 발견해 수정했습니다. 이 작업을 먼저 하지 않았다면 AI 결과가 정말 정확한지 아닌지도 알 수 없었을 것입니다.

**아직 확인하지 못한 것**: 이런 오류 패턴이 실제 GA 상담 데이터에서 얼마나 자주 나타나는지, 그리고 실제 설계사가 이 도구를 썼을 때 업무에 어떤 효과가 있는지는 아직 확인하지 못했습니다. 자세한 내용은 §7에서 다룹니다.

**실행 범위**: 설계한 15개 테스트 케이스 중 7개(TC-01, 03, 04, 05, 10, 11, 15)를 실제로 실행했습니다. 그중 5개(TC-03, 05, 10, 11, 15)는 세 번째 baseline(지침 동일 + 자유서술)까지 추가로 실행했습니다. 평가 기준은 `03_평가설계.md`/`02_테스트케이스.md`에서 확정한 6축으로, Recall(Weighted Recall 포함), Precision, Evidence Accuracy, Unknown Accuracy, Conflict Accuracy, Human Correction입니다.

---

## 2. 최종 수치

AI가 낸 결과를 구조화 파이프라인으로 실제 실행해 자동 채점한 수치는 다음과 같습니다. 어떻게 이 수치를 얻었는지는 §3~§6에서 하나씩 설명합니다.

| TC | Recall | Precision | Evidence | AUTO-FAIL | HUMAN-QUEUE |
|---|---|---|---|---|---|
| TC-01 | 3/4 (0.75) | 1.0 | 1.0 | 1 (B4) | 0 |
| TC-03 | 0/1 (0.0)* | 1.0 | 1.0 | 0 | 4 |
| TC-04 | 2/2 (1.0) | 1.0 | 1.0 | 2 (A8, A11) | 1 |
| TC-05 | 1/1 (1.0) | 1.0 | 1.0 | 1 (D3) | 1 |
| TC-10 | 1/1 (1.0) | 1.0 | 1.0 | 0 | 0 |
| TC-11 | 2/2 (1.0) | 1.0 | 1.0 | 1 (D4) | 1 |
| TC-15 | 3/4 (0.75) | 1.0 | 1.0 | 4 (A11,D3,D4×2) | 2 |

\* TC-03의 Recall이 0/1로 나온 것은 AI가 값을 틀려서가 아닙니다. `source_id`를 잘못 기재해
M1 매칭이 실패했을 뿐이고, M2로 값 자체는 일치한다는 게 확인됐습니다. `03_평가설계.md`
§1-4에서 정한 M1/M2/M3 설계에 따라 이런 경우는 HUMAN-QUEUE로만 처리합니다.

**Naive vs Structured vs Instructed-freeform** 요약 비교는 §4에서 다루고, 전체 수치는
`results/metadata/*.json`과 두 human-review 문서 원본에서 확인할 수 있습니다.

---

## 3. 평가기 자체 검증

§1-3("채점 도구 자체도 믿을 수 없어서, 그것부터 검증했다")의 근거가 되는 절입니다. 실제로 N=7을 순서대로 돌리면서, 평가기 코드 자체의 결함 9건을 발견해 수정했습니다.

| # | 문제 | 발견 계기 | 조치 |
|---|---|---|---|
| 1 | 인코딩 크래시 | Windows 콘솔 실행 | `stdout` UTF-8 재설정 |
| 2 | B1 오분류(환각 vs 출처오귀속 미구분) | TC-03 | `ground_status()`를 `grounded`/`wrong_source`/`not_found` 3분류로 분리 |
| 3 | M1 단독 매칭이 너무 엄격함(source_id 오귀속 시 Recall 0) | TC-03 | M2(값 기반 폴백, HUMAN-QUEUE 전용) 도입 |
| 4 | D1/D2 conflict 매칭 키 버그(`match_key()`를 conflict 객체에 오적용) | TC-04 | `sources[]` 내부 키 집합 비교로 교체 |
| 5 | 중복매칭으로 Recall 150% | TC-11 | `consumed_gt_ids`로 GT 항목당 1회만 인정 |
| 6 | D3(Conflict Recall) 규칙 부재 — conflict를 놓치고 임의 확정해도 무검출 | TC-05 | Conflict Recall(D3) 규칙 신설 |
| 7 | D4/A11 규칙 부재 — unknown 누락·자기모순 무검출 | TC-15 | Unknown Recall(D4) + 자기모순 검사(A11) 신설 |
| 8 | B5(값-근거 일치) 규칙 부재 — 근거 문장은 진짜인데 선언값이 달라도 무검출 | 회귀 테스트(999,999원 값 위조 주입) | 값-근거 정합성(B5) 신설 |
| 9 | 재현성 버그 — 원본 평가결과 파일에 교정본 평가가 남아있었음 | B5 작업 중 | `ai_output_path` 인자로 임의 파일 지정 평가, 출력 파일명 폴더명 접두 분리 |

**수정 후 확인**: N=7 전체를 재실행해서 새로 추가한 규칙(B5 등)이 기존에 통과했던 케이스에 새로운 오탐을 만들지는 않는지 확인했습니다. fixture 3종(정상/실제conflict/값위조)으로 수정 전과 후도 서로 대조했습니다(자세한 기록은 `results/evaluator_validation_log.md` 참고).

---

## 4. AI 결과 검증

평가기 자체의 신뢰성(§3)을 확인했으니, 이제 그 평가기로 실제 AI 결과를 채점합니다.

**Structured**: 7건을 실행해 자동으로 채점했습니다(수치는 §2의 표 참고). Evidence 원문 존재율은 7건 모두 100%로, 환각은 한 건도 없었습니다.

**Naive Baseline**: 스키마 없이 자유 텍스트로 7건을 실행한 뒤, 사람이 원문·GT와 직접 대조해 6개 기준(변경사항 발견/방향·값 정확/근거 명시/추론 안 함/충돌 보존/유불리 미판단)으로 채점했습니다. 근거 명시는 7건 중 한 건도 완전히 충족하지 못했고, 출처 충돌 보존은 3건 모두 실패했으며, 유불리 미판단만 7건 중 1건에서 지켜졌습니다. structured와 가장 크게 갈린 지점이 바로 이 부분입니다(자세한 내용은 `results/naive_baseline_human_review.md` 참고).

**변경사항 발견(Recall)은 원래 "차이가 크지 않다"고 썼는데, 다시 계산해보니 그것도 정확하지 않았습니다.** naive의 "6/7"은 케이스 단위 관대한 판정이고 structured의 "0.75~1.0"은 항목 단위 엄격한 비율이라, 애초에 같은 기준으로 잰 게 아니었습니다. GT `changes` 항목 15개를 기준으로 둘 다 항목 단위로 다시 채점하면 naive 14/15, structured도 매칭 아티팩트(TC-03 출처 오귀속, TC-15 paraphrase 미매칭)를 콘텐츠 기준으로 보정하면 14/15로 **완전히 동률**입니다. naive의 유일한 진짜 오류(TC-15 뇌혈관질환 보장 삭제 완전 누락)는 사람이 원문과 대조해야만 드러났지만, structured의 유일한 진짜 오류(TC-01)는 평가기의 B4 규칙이 자동으로 검출했습니다. 상세 재분석은 `results/naive_vs_structured_recall_reanalysis.md` 참고.

**Instructed Freeform Baseline**: structured와 동일한 지침(근거·충돌·판단경계)을 주되 스키마 강제 없이 자유 텍스트로 받은 세 번째 축입니다. held-out 5건(TC-03,05,10,11,15)에 실행해보니, naive가 실패했던 부분의 상당수는 스키마가 아니라 지침이 없었기 때문이라는 게 확인됐습니다(TC-05는 conflict를 명시적으로 분류했고, TC-10은 환각을 만들지 않았으며, TC-15는 Critical 항목을 포착했습니다). 다만 5건을 1회만 실행한 결과라 이 결과를 일반화하지는 않습니다(자세한 내용은 `results/instructed_freeform_human_review.md` 참고).

**Human Review**: TC-05에서 AI는 conflict를 놓치고 35%라는 값을 임의로 확정해버렸는데, 이 문제를 evaluate.py가 D3 AUTO-FAIL로 정확히 검출했습니다. 그래서 사람이 원문 2건을 직접 대조해 conflicts로 다시 분류했고, 재검증에서 D3 문제는 사라졌지만 예상하지 못했던 D2 오탐이 새로 발견됐습니다(§7 참고). 자세한 기록은 `results/corrected/TC-05.json`에 있습니다.

---

## 5. Conflict 검증

§4에서 naive가 출처 충돌 보존에서 가장 크게 뒤처졌다고 했는데(0/3), 그 실패 패턴을 좀 더 구체적으로 들여다봅니다.

**계약 데이터와 상담 내용의 충돌(source_discrepancy)**: 설계사 발화와 실제 계약 데이터가 어긋나는 경우입니다. TC-04("암 보장은 그대로예요" vs 실제 3,000만→2,000만 축소)에서는 D1 규칙이 정상적으로 검출했고, TC-15("상해 보장 그대로예요" vs 실제 1,000만→500만 축소)에서는 D3가 AI의 미검출을 정확히 잡아냈습니다. AI가 이 유형을 매번 놓치는 건 아니지만, 재현 가능한 실패 패턴이라는 걸 보여줍니다.

**GT 없는 Conflict Detector 검증 결과**: `src/conflict_detector.py`는 GT도 LLM도 쓰지 않고, 원문 텍스트(%/원 수치 + 한글 2-gram 유사도)만으로 문서 간 충돌을 탐지합니다. TC-05(40% vs 35%)를 정확히 탐지했고, 나머지 6건에서는 오탐이 한 건도 없었습니다(TC-04/15의 conflict는 발화-데이터 불일치 유형이라 이 탐지기가 다루는 범위가 아닙니다 — 의도된 범위 제한입니다). 자세한 내용은 `results/conflict_detector_log.md`에 있습니다.

---

## 6. 반복 검증 과정

지금까지의 검증 과정 자체도 처음부터 매끄러웠던 건 아닙니다. TC-01 하나를 두고 반복한 과정을 기록해둡니다.

**TC-01 v1 → v2**: 첫 실행(v1)에서 4개 실질 변화 중 3개가 `changes`/`unchanged_items` 배열에 정반대로 들어가는 오류를 발견했습니다. `old`/`new`/방향 값 자체는 100% 정확했기 때문에, 비교 능력이 아니라 배열 라우팅만 실패한 것으로 확인됐습니다. 원인을 "필드명 `change`와 배열명 `changes`의 문자열 충돌"로 가설을 세우고, `delta_type`으로 이름을 바꾸고 배치 규칙을 명시하고 5단계 자체검사 절차를 추가해 재실행(v2)한 결과 3/4가 개선됐습니다. 잔여 1건은 원인을 가설로만 남기고 v3를 만들지 않기로 결정했습니다. 프롬프트를 오류가 없어질 때까지 계속 고치는 것 자체가 이 프로젝트의 메시지(오류를 어떻게 감지·통제하는가)에서 벗어난다고 판단했기 때문입니다. 이 잔여 오류는 이후 실제로 평가기의 B4 규칙이 정확히 검출했습니다. 자세한 기록은 `results/tc01_iteration_log.md`에 있습니다.

---

## 7. 확인된 한계

§2의 수치가 전부는 아닙니다. 스스로 확인한 한계도 함께 남겨둡니다.

- **15개 중 7개만 실행, 그중에서도 한 문제정의 카테고리는 특히 약함**: 나머지 8개
  (TC-02,06,07,08,09,12,13,14)는 설계와 GT까지는 완성했지만 아직 실행하지 않았습니다.
  `01_문제정의.md`가 정의한 6개 비교 항목(보장 축소·지급조건·갱신기간·해지환급금·면책사항·
  기존 계약 해지 불이익) 중 앞의 5개는 실행된 7건에서 최소 1건 이상 직접 검증됐지만, 마지막
  "기존 계약 해지 불이익"은 전담 케이스(TC-07, TC-13)가 둘 다 미실행 목록에 있어 복합
  케이스인 TC-15에서만 간접적으로 다뤄졌습니다. 그리고 그 유일한 기회에서 AI는 이 항목
  (`existing_contract_dissolution_impact`)을 실제로 놓쳤고, 평가기의 D4 규칙이 이를 정확히
  검출했습니다(`results/metadata/TC-15_eval.json`). 이 카테고리는 "검증됐다"고 말할 수
  없고, 오히려 유일한 증거가 실패 사례입니다.
- **Weighted Recall 미구현**: 공식과 GT 가중치 데이터(`weighted_recall_target_items`)는
  준비돼 있지만, `evaluate.py`가 아직 이 값을 읽지 않습니다. 위 표의 모든 Recall 수치는
  unweighted 기준입니다.
- **paraphrase 매칭 미구현**: 표현은 다르지만 의미가 같은 경우(TC-15의
  `coverage_scope_cancer`)는 문자열 기반 매칭에서 미매칭으로 판정됩니다. 오탐 위험이 있고
  임계값을 정할 근거도 부족해서 의도적으로 보류했습니다.
- **D2 false positive**: 같은 문서를 인용하는 무관한 항목을 중복 확정으로 잘못 판단하는
  경우가 있습니다(TC-05 Human Review 검증 중 발견). 발견하자마자 "평가기를 완벽하게 다듬지
  않는다"고 판단해서 의도적으로 미해결 상태로 남겼습니다.
- **baseline 표본 제한**: 세 번째 baseline(지침+자유서술) 비교는 held-out 5건을 1회만
  샘플링한 결과라서 일반화하지 않습니다.
- **구조화 파이프라인도 사실 1회 샘플입니다**: 위 §6 표를 포함해 이 문서의 구조화 파이프라인
  수치 전부 `src/llm_extractor.py`가 `temperature` 고정 없이(OpenAI 기본값) 낸 단 1회
  실행 결과입니다. 세 번째 baseline에는 "1회 샘플링, 일반화 안 함"을 명시해뒀으면서 정작
  메인 표에는 같은 경고가 빠져 있었습니다 — 실제로 `linked_need_id` 작업 중 TC-15를
  재실행해보니 conflicts 검출 여부가 달라지는 걸 확인하면서 뒤늦게 발견했습니다(2026-09-17).
  이후 `temperature=0`을 추가해 재현성을 개선했지만(완전한 결정성 보장은 아님), 위 표의
  기존 수치를 재실행으로 다시 확정하지는 않았습니다 — 표는 그 시점 1회 실행의 기록으로
  남겨둡니다.
- **실제 사용자 검증 미실시**: 설계사가 실제로 이 도구를 썼을 때 시간이 얼마나 줄고 누락이
  얼마나 줄어드는지는 가상 시나리오 기반 추정치일 뿐이고, 실사용자 테스트는 아직 하지
  않았습니다. 제3자 블라인드 테스트 대신 실행 가능한 대안(실험자 1인 counterbalanced
  자기실험)을 설계는 해뒀지만 아직 실행 전입니다. 설계와 편향 완화 절차, 사전 등록한 해석
  규칙은 `05_업무효과검증_설계.md`에 정리해뒀습니다.

---

### 상세 검증 기록

- 평가기 자체 검증: `results/evaluator_validation_log.md`
- Naive Baseline Human Review: `results/naive_baseline_human_review.md`
- Naive vs Structured 항목 단위 Recall 재분석(기존 주장 번복 기록): `results/naive_vs_structured_recall_reanalysis.md`
- Instructed Freeform Baseline Human Review: `results/instructed_freeform_human_review.md`
- GT 없는 Conflict Detector 검증: `results/conflict_detector_log.md`
- TC-01 반복 검증(v1→v2): `results/tc01_iteration_log.md`
